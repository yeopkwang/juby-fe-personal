# 수정 결과 2026-09-23

## 기준
- 시작 커밋 `e4bbf64` / 끝 커밋 `91d638c` (브랜치 `feat/login-and-stock-detail`, 커밋 13개, 푸시 안 함)
- 각 커밋 뒤 lint · tsc · build: **13개 모두 통과** (`npm run lint` → `npx tsc -b` → `npm run build`가 모두 통과해야 커밋하도록 한 줄로 묶어 돌렸다)
- 검증 방식: 지난 검사와 같다. 화면 동작은 운영 빌드(`vite preview`)에 Playwright, 함수 단위는 개발 서버가 변환한 원본 모듈을 브라우저에서 직접 불러 실행. 테스트 러너·의존성은 들이지 않았다
- 실제 백엔드 호출: `GET /api/personality-tests` 7회, `GET /api/backtest/preset/options` 4회, `GET /api/backtest/preset` 12회. **증권사(KIS)를 거치는 경로는 0회** — 나머지는 전부 가로챘다
- 작업마다 거친 순서: ① 고치기 전 빌드에서 판정이 `실패`인지 확인 → ② 고침 → ③ 같은 판정으로 `통과` 확인 → ④ 고친 파일만 `git stash`로 잠깐 빼고 다시 빌드해 `실패`로 돌아가는지 확인 → ⑤ 원상 복구 후 lint·tsc·build → 커밋

## 작업별
| 작업 | 커밋 | 재검증 ID | 전 → 후 | 되돌렸을 때 다시 실패하나 | 비고 |
|---|---|---|---|---|---|
| 1 | `11cb1be` | M-상태-성향404 | 실패 → 통과 | 예 | 성향 조회는 이미 `getMyPersonality()` 하나로 모여 있었다(마이페이지·AI·백테스트 모두 사용). 그래서 `member.ts`에서 404 `MEMBER404_2`만 `null`로 바꿨다. AI·백테스트는 `.catch(() => null)`로 모든 실패를 "성향 없음"으로 받는 기존 동작을 그대로 두었다(지시서: 동작 불변). 지시서가 가리킨 `AiPage.tsx:145`는 성향 조회가 아니라 **질문 POST**의 404 처리라 합칠 대상이 아니다 |
| 2 | `2d0fcb9` | P-상태-저장실패(500) · P-상태-저장끊김 | 실패 → 통과 · 실패 → 통과 | 예 · 예 | "다시 저장" 버튼을 따로 두지 않고 마지막 문항의 "결과 보기" 버튼 글자를 "다시 저장"으로 바꿨다. 같은 일을 하는 버튼이 두 개 생기지 않게 하려고서다. 안내는 버튼 위 `role="alert"`. 답을 바꾸면 안내가 사라진다. 다시 저장은 같은 본문으로 POST 1회(측정: 본문 같음, 성공 시 결과 화면) |
| 3 | `d169672` | P-상태-500-로그인 · 끊김-로그인 · null-로그인 · 빈배열-로그인 · P-상태-500-비로그인 · 빈배열-비로그인 | 모두 실패 → 통과 | 예(6건 모두) | 원 검사의 P-상태-500 등은 "안내 또는 예비 문항이면 통과" 기준이라 `통과`였다. 이번 지시서의 새 기준(로그인은 예비 문항 금지, 비로그인은 임시 문항 안내)으로 재서 `실패`로 재현했다. `personality.ts:98-100`(환산값 하나를 POST)은 고치지 않고 주석만 정정했다 — 로그인 상태에서 예비 문항으로 가는 길을 막았으니 그 분기는 `USE_BACKEND_QUESTIONS=false`인 개발 상태에서만 닿는다. CLAUDE.md의 예비 문항 설명도 고쳤다 |
| 4a | `2a8af1f` | W-FN-5 · H-상태-빈문자열 · 추가 발견 2(baseDate null) · 관심-baseDate-빈값 | 실패(26/60) → 통과(60/60) · 실패 → 통과 · 실패 → 통과 · 실패 → 통과 | 예 · 예 · 예 · 예 | 날짜가 비면 홈 기준일·관심종목 기준일·상세 "마감 기준" 줄을 줄째 숨긴다. 관심종목 기준일은 지시서에 없던 같은 증상이라 함께 쟀다. **추가 발견 1(목록 한 행·상세의 필드 누락)은 이 커밋만으로 통과**했다(4b 직전 측정에서 통과) |
| 4b | `eeeae74` | B-상태-null · B-상태-지표null · B-상태-finalScore없음 · M-상태-null · W-API-4 | 모두 실패 → 통과 | 예(5건 모두) | 목록 행 단위 가드는 두지 않았다(지시서: 한 행으로 목록 전체를 실패시키지 않음. 4a가 "-"로 그린다). 200 + HTML 본문은 지시 범위(빈 본문·래퍼 없음) 밖이라 기존대로 `Error`다(CLAUDE.md의 설계). CLAUDE.md에 새 규칙 한 줄 추가 |
| 4c | `048603c` | W-R-2 — 4c-백테스트 · 4c-마이페이지 · 4c-홈 · 4c-상세(4a·4b를 되돌린 상태에서 잼) | 4건 모두 실패(화면 전체 오류) → 통과(그 구역만 안내 + 다시 시도로 데이터 재요청) | 예(4c까지 빼면 0/4) | **지시서에 없는 구조 변경 1건**: 종목 상세 가격·차트 구역을 같은 파일 안의 자식 컴포넌트 `PriceSection`으로 떼어냈다. 오류 경계는 **자식이 그리다 난 오류만** 받아내는데, 이 구역의 계산·JSX가 `StockChartPage` 자신의 렌더에 있어 경계를 지나쳤기 때문이다(떼기 전 측정: 4c가 있어도 상세만 화면 전체 오류). 동작은 같다. 끝 상태에서도 가드를 잠깐 빼고 다시 재 네 구역 모두 통과를 확인했다. CLAUDE.md에 구역 경계 설명 추가 |
| 5 | `98e5282` | B-상태-500(예외 문구) · B-상태-500-서버문구 · M-수정400 | 모두 실패 → 통과 | 예(3건 모두) | grep으로 찾은 두 번째 곳(`MypageProfilePage.tsx` 400 → 서버 message)을 같이 고쳤다. 400은 서버가 거는 두 규칙(이름 2~4자, 생일은 오늘 이전)을 적은 고정 문구로 바꿨다. `BACKTEST404_5`·`BACKTEST400_1` 문구는 그대로다(회귀 통과) |
| 6 | `67147a7` | L-3(8경우 + 앞뒤 공백 토큰) · L-쓰레기토큰 | 실패 → 통과 · 실패 → 통과 | 예 · 예 | 규칙을 `auth.ts`의 `toUsableToken` 한 곳에 두고, 콜백뿐 아니라 **토큰을 읽는 자리(`readToken`)에도** 적용했다. `isLoggedIn`만 바꾸면 이미 저장된 `"null"` 토큰이 요청 헤더에 `Bearer null`로 계속 실려 공개 API까지 401로 막힐 수 있어서다(L-쓰레기토큰으로 확인). 앞뒤 공백이 있는 정상 토큰(`" a "`)은 잘라서 저장한다 |
| 7 | `bc20756` | S-1-abc · S-1-5자리 · S-1-7자리 · S-1-한글 | 모두 실패(요청 3건) → 통과(요청 0건) | 예(4건 모두) | `NewsList`는 고치지 않았다. 형식이 틀린 코드면 렌더에서 곧바로 "목록에 없는 종목"을 돌려줘 `NewsList`가 아예 마운트되지 않는다. effect에서 막는 방식은 쓰지 않았다 — 첫 렌더에 `NewsList`가 먼저 마운트되고 자식 effect가 부모보다 먼저 돌아 뉴스 요청이 나가기 때문이다. `/stocks/000000`은 지금처럼 요청 1회 후 404 안내(회귀 통과) |
| 8 | `23e847c` | 추가 발견 4(함수 8경우) · A-1 | 실패 → 통과 · 실패 → 통과 | 예 · 예 | 검색창의 `normalize()`를 내보내 그대로 썼다. 가장 긴 이름은 정규화한 길이로 고른다. 검색창 회귀 검사 7/7 통과 |
| 9 | `52db326` | S-뉴스-더보기실패 · S-4 · S-뉴스-제목빈문자열 · M-상태-해제실패 · H-하트실패 · A-2-빈문자열 · A-2-null · A-2-공백 · H-3 · H-상태-빈배열 · P-상태-보기빈배열-비로그인 · P-상태-보기빈배열-로그인 | 12건 모두 실패 → 통과 | 예(12건 모두) | 홈 하트(추가·해제 실패)도 관심종목 해제와 같은 방식으로 알린다(추가 실패는 "관심종목에 추가하지 못했어요"). `home.ts`는 카드 실패 이유를 알리는 콜백 하나만 더했다. 빈 시세표 안내는 표 몸통(`ul`) 안의 한 줄로 넣었다(밖에 두면 빈 테두리 선만 남음) |
| 9 후속 | `91d638c` | S-뉴스-더보기끝 | 실패 → 통과 | 예 | **작업 9에서 내가 만든 회귀**를 찾아 고쳤다(아래 추가 발견 1). 한 작업 한 커밋 규칙에서 벗어나지만, 이미 커밋한 작업 9를 되감지 않으려고 후속 커밋으로 두었다 |
| 10(4절) | `8e21419` | 추가 발견 11 · P-4(W-FN-7 포함) · B-5 · W-FN-3 · C-3 · 추가 발견 5 · W-D-4 · W-S-1 | 모두 실패 → 통과 | 예(8건 모두) | 공백 대체는 `textOr()` 하나로 두 곳에서 쓴다. 적합도 등급은 화면과 같은 `toFixed(1)` 값으로 가른다. knip이 작업 3에서 내가 내보낸 `QuestionSet` 타입까지 잡아 같이 정리했다(안 쓰는 export 3·타입 4 → 0). CSS 검사기가 한 CSS를 여러 파일이 같이 쓰는 경우를 합치지 않아 생기던 오탐(11건)을 고친 뒤 판정했다(기준선 3 → 0). 작업 9에서 `.likeNotice`를 `.asOf` 설명 주석 아래로 잘못 끼워 넣은 것도 여기서 바로잡았다 |

### W-R-4 표 다시 채움 (로딩 / 실패 / 빈 값)
| 컴포넌트 | 로딩 | 실패 | 빈 값 |
|---|---|---|---|
| 홈 시세표 | O 불러오는 중… | O 불러오지 못했습니다 + 다시 시도 | O 표시할 종목이 없어요 |
| 홈 테마 카드 | O 반짝이는 자리표시 | O "불러오지 못했어요"(반짝임 없음) | O "표시할 값이 없어요"(반짝임 없음) |
| 종목 상세 | O 스켈레톤 | O 불러오지 못했습니다 + 다시 시도 | O 이 기간에는 거래일이 없습니다 |
| 뉴스 | O 불러오는 중… | O 상자 안 안내 + 다시 시도 / O 더 보기 실패 안내 | O 관련 뉴스를 찾지 못했습니다 |
| 성향검사 문항 | O 불러오는 중… | O 로그인: 실패 안내 + 다시 시도 / 비로그인: 임시 문항 안내 | O 보기 빈 문항도 같은 처리 |
| 성향검사 저장 | — (버튼 잠금) | O 저장 실패 안내 + 다시 저장, 답 유지 | — |
| 백테스트 결과 | O 불러오는 중… + 버튼 잠금 | O 코드별 문구 / 그 밖엔 고정 문구 + 다시 시도 | O result·지표 null도 실패 안내 |
| AI 답 | O 답변을 생성하고 있어요 + 입력 잠금 | O 답변을 가져오지 못했어요 + 다시 시도 | O 빈 답도 같은 안내 |
| 내 정보 | O 스켈레톤 | O 문구 + 다시 시도 | O result null도 실패 안내 |
| 투자유형 | O 스켈레톤(이번에 잼) | O 문구 + 다시 시도 | O 404·null 모두 "아직 검사하지 않았습니다" |
| 관심종목 | O 스켈레톤(이번에 잼) | O 문구 + 다시 시도 / O 해제 실패 안내 | O 아직 관심종목이 없어요 |

**X 11곳 → 0곳.** 지난번 "재지 않음" 두 칸(투자유형·관심종목 로딩)도 이번에 재서 채웠다.

## 회귀 확인
- **기존 `통과` 92건 전부를 끝 상태(`8e21419` 빌드)에서 다시 돌렸다 → 92/92 통과.**
  - 첫 실행은 90/92였다. 실패 2건은 둘 다 검사 쪽 문제였고, 검사를 고쳐 다시 재니 통과했다.
    - W-AUTH-2: 개발 서버를 켠 채 `auth.ts`를 고쳐서, 앱은 `?t=` 붙은 새 모듈을 쓰는데 검사는 붙지 않은 주소로 불러 다른 모듈 인스턴스에 구독했다. 서버를 새로 띄우니 알림 1회·이동 1회로 통과했다.
    - W-R-5: grep이 `${index}`의 `$`를 정규식 기호로 읽어 뉴스 목록 key를 못 잡았다. 고정 문자열 검색으로 두 곳이 그대로 있음을 확인했다.
  - 이름이 바뀐 판정과 짝지은 것: P-상태-500·끊김·null·빈배열 → 작업 3의 로그인·비로그인 판정(원래 기준 "안내 또는 예비 문항"을 새 기준이 포함한다), W-API-5 → W-API-4 표(200 + isSuccess:false가 ApiError), L-4 → L-2(뒤로가기 목적지까지 본다).
  - 실제 호출로 다시 잰 것: P-1(문항 10×5), B-1(전략별 기간 목록), B-2·B-2-보조(화면 숫자 = 응답 값, preset 6건).
  - M-5는 값으로 넣은 미래 날짜(막힘)와 정상 날짜(저장)만 다시 쟀다. 키보드로만 만들 수 있는 없는 날(2/30)은 이번 수정이 건드리지 않은 브라우저 기본 검증 경로라 첫 검사 결과를 그대로 둔다.
- 끝 커밋(`91d638c`) 뒤 다시 잰 것: 뉴스 판정 7건(더보기끝·더보기실패·제목빈문자열·정상·S-4·S-5·빈배열) 통과, C-2(운영·개발 각 11개 화면 콘솔 에러·경고 0건) 통과.
- 작업마다 건드린 화면의 기존 통과 항목은 해당 작업의 검증에 함께 넣어 전후를 쟀다. 예: 작업 1은 A·백테스트 성향 판정 6건, 작업 4b는 B-2 녹화 2건·S/H 실패 안내 4건, 작업 9는 A-4·H-2·P-1 등 8건. 모두 `통과 → 통과`였다.
- 이번에 새로 만든 작업별 판정 63건도 끝 상태에서 한 번에 다시 돌렸다 → 59/63. 남은 4건은 4c 판정인데, 이 판정은 가드를 빼서 일부러 그리기 오류를 낸 상태에서 재도록 만든 것이라 오류가 안 나는 끝 상태에서는 구역 안내가 안 뜨는 게 맞다. 그래서 가드 세 줄을 잠깐 빼고(타입 검사 없이 `vite build`) 다시 재 4/4 통과를 확인한 뒤 되돌렸다.

## 재현이 안 된 것 / 고치지 않은 것
- **재현이 안 된 지시 항목은 없다.** 지시서의 ID는 모두 고치기 전 빌드에서 `실패`로 재현한 뒤 고쳤다.
- 추가 발견 1(목록 한 행·상세 필드 누락): 4a에서 해결돼 4b 직전 측정에서 이미 통과했다. 그래서 4b에 행 단위 가드는 두지 않았다.
- `personality.ts:98-100`(작업 3), `NewsList` 요청 시작부(작업 7), `AiPage.tsx:145`(작업 1): 고치지 않은 이유는 위 작업별 비고에 적었다.
- 5절 여섯 항목(B-6, 추가 발견 14, B-3·추가 발견 8, H-4, W-FN-1, W-B-4): 지시대로 손대지 않았다. W-FN-1은 작업 9에서 보기 빈 문항을 문항 조회 실패로 막아, NaN이 생기던 유일한 경로가 사라진 것을 확인했다(P-상태-보기빈배열-로그인·비로그인).

## 추가 발견
1. **작업 9가 만든 회귀 — 고침(`91d638c`).** 제목·링크가 빈 기사를 목록에서 빼자, "더 보기" 판단(`보여준 개수 < totalCount`)이 끝에 닿아도 모자라 보여 버튼이 남았다. 누를 때마다 빈 페이지를 요청했다(실측: 전체 25건 중 1건을 거르면 3·4·5쪽을 계속 요청). 이제 거르기 전 받은 개수로 판단한다.
2. **백엔드는 처리 안 된 예외의 문자열을 `message`가 아니라 `result`에 싣는다**(`GeneralExceptionAdvice.java:83-87`, origin/dev). `message`는 "서버 에러…" 같은 고정 문구다. 그래서 지난 보고서 추가 발견 3("java.lang… 노출")은 지금 서버에서는 일어날 가능성이 낮았다. 작업 5는 방어로서 그대로 의미가 있다.
3. `HomePage.module.css`의 `.asOf` 위 주석 "지난 장 값을 보여주는 중이라는 표시"는 지금 동작("9월 22일 종가 기준" 칩)과 맞지 않는다. 지시서 밖이라 그대로 두었다.
4. 콘솔 호출 수가 바뀌었다: console.warn 22 → 23(AI 빈 답), console.error 1 → 2(구역 경계). console.log는 0 그대로다.
5. 검사 도구에서 바로잡은 것. 앱 문제가 아니라 이 보고서의 측정을 믿어도 되는지와 관련된 것이다.
   - `git stash`는 새로 만든(추적 안 되는) 파일을 경로로 못 가져간다. 그래서 4c 첫 되돌림 측정이 무효였고, `-u`로 다시 쟀다.
   - 4c 첫 측정에서 상세가 실패한 것은 측정이 아니라 실제 구조 문제였다 → `PriceSection` 분리(작업별 4c 비고).
   - 끝 상태 4c 재확인 첫 시도는 임시 수정이 `tsc`에 걸려 빌드가 안 된 채 옛 빌드를 잰 것이라 무효였다. `vite build`로만 묶어 다시 쟀다.
