# 테스트 결과 2026-09-23

## 기준
- 커밋: `e4bbf64` (브랜치 `feat/login-and-stock-detail`, 작업 트리 깨끗함)
- 저장소: `yeopkwang/juby-fe-personal` — 지시서의 `JUBYInvest/JUBY-FE`와 다르다(D-1)
- lint / tsc / build: **통과** (`npm ci` → `npm run lint` → `npx tsc -b` → `npm run build` 모두 exit 0, 출력은 맨 아래 부록)
- 실행 환경: 블랙박스는 운영 빌드(`vite preview`, React StrictMode 없음)에 Playwright(Chrome), 함수 단위 검사는 개발 서버가 변환한 원본 모듈을 브라우저에서 직접 불러 실행. 테스트 러너는 들이지 않았고 `package.json`·소스는 건드리지 않았다
- 실제 백엔드 호출: `GET /api/personality-tests` 4회, `GET /api/backtest/preset/options` 7회, `GET /api/backtest/preset` 18회, 스웨거 `GET /v3/api-docs` 1회. **증권사(KIS)를 거치는 경로는 0회** — 나머지는 전부 가로채 응답을 지어 줬고, 백엔드 주소로의 주소 이동(소셜 로그인)도 가로챘다
- 진행 방식: 각 항목의 예상은 실행 전에 스크립트에 고정했다. `통과`는 반대 상황(응답을 망가뜨리거나 기대값을 틀리게)에 같은 판정식을 대 거짓이 나오는 것까지 본 것만 적었다. 판정식을 잘못 짠 경우(탐지 문자열이 좁았던 S-2·S-뉴스-빈배열·C-3 등)는 기대 결과는 그대로 두고 탐지 방식만 고쳐 다시 쟀고 해당 칸에 적었다
- 한계: 수행자는 이 코드를 앞선 작업에서 다룬 적이 있어 블랙박스가 완전히 "모르는 상태"는 아니다. 스크린샷·스크립트는 세션 임시 폴더에 있다(아래 근거 칸의 파일명)

## 요약
| 판정 | 개수 |
|---|---|
| 통과 | 92 |
| 실패 | 25 |
| 보류 | 3 |
| 사람 확인 | 13 |
| 문서와 다름 | 23 |

가장 먼저 볼 것 셋: ① 응답에 null이나 빠진 필드가 하나만 있어도 **화면 전체가 오류 화면**이 되는 곳이 4곳(백테스트 결과, 내 정보, 홈 시세표, 종목 상세 — B-상태-null·M-상태-null·추가 발견 1) ② 로그인 상태에서 성향검사 **저장이 실패하면 답 10개가 사라지고 막다른 화면**(P-상태-저장실패) ③ 갓 가입해 성향이 없는 회원이 마이페이지에 들어가면 **오류 문구**를 본다(M-상태-성향404).

## 실패 (먼저 볼 것)
심각한 순서. "원인" 칸은 코드를 읽어 확인한 것과 추측을 나눠 적었다.

| ID | 예상 | 실제 | 재현 | 위치 | 원인 확인/추측 |
|---|---|---|---|---|---|
| B-상태-null · B-상태-지표null | 백테스트 응답의 result가 null이거나 지표 하나(profit)가 null이어도 화면이 살아 있고 안내가 나온다 | 화면 전체가 오류 화면("화면을 표시하지 못했어요")으로 바뀐다. 입력 폼·가이드까지 사라짐 | /backtest → 삼성전자·이동평균 교차·1년 → 시작. preset 응답을 {isSuccess:true,result:null} 또는 result.result.profit=null로 가로챔 | src/pages/BacktestPage.tsx:251-261(받은 preset을 검사 없이 ready로), 561·568(preset.investType, calculateAxisScores) → src/utils/backtest.ts:224(result.profit.totalReturn) | 확인: 코드 경로와 콘솔 TypeError. 비교용 5건은 .then 안에서 던져 allSettled가 걸러 안전 |
| M-상태-null | 회원정보 응답 result: null이어도 화면이 살아 있다 | 화면 전체가 오류 화면. 콘솔 TypeError: Cannot read properties of null (reading 'birth'). 마이페이지 사이드바·로그아웃 버튼도 사라짐 | 토큰을 넣고 /mypage/profile, GET /api/members/me를 {isSuccess:true,result:null}로 가로챔 | src/pages/MypageProfilePage.tsx:72(null도 ready로), 176(member.birth) · src/api/member.ts:14 | 확인: 콘솔 스택과 코드 |
| P-상태-저장실패 | 로그인 상태에서 결과 저장 POST가 실패하면 결과는 보여주되 저장 안 됐음을 알린다 | 10문항을 다 답하고 "결과 보기" → POST 500이나 끊김이면 결과 화면 대신 "문항을 불러오지 못했습니다."만 남고 버튼·링크 0개. 고른 답 10개가 화면에서 사라진다 | 토큰을 넣고 /personality-test를 끝까지 답함, POST /api/personality-tests를 500으로 가로챔 | src/pages/PersonalityTestPage.tsx:79-81(저장 실패에 문항 조회 실패와 같은 hasError를 켬) → 43-44(화면 전체를 그 문구로 교체) · src/api/personality.ts:98-100(비로그인용 프론트 채점이 있는데 실패 시 쓰지 않고 던짐) | 확인: 코드 경로. 401은 전역 처리로 /login(정상 경로) |
| M-상태-성향404 | 성향 없음(404 MEMBER404_2)이면 "검사하러 가기"로 안내한다 | "투자성향을 불러오지 못했습니다. 잠시 후 다시 시도해 주세요. 문제가 계속되면 다시 로그인해 보세요." + 다시 시도 | 토큰을 넣고 /mypage(기본 탭이 투자유형), GET /api/members/me/personality를 404 MEMBER404_2로 가로챔 | src/api/member.ts:35-41(404를 "없음"으로 바꾸지 않고 던짐) → src/pages/MypagePersonalityPage.tsx:31-33(error 상태) | 확인: 백엔드 MemberService.getPersonalityInfo(origin/dev)가 성향 없는 회원에게 이 404를 준다 = 갓 가입한 모든 회원. 같은 404를 /ai(AiPage.tsx:145)와 /backtest는 제대로 받는다 |
| L-3 | 콜백 토큰이 빠지거나 빈 값이면 저장하지 않고 에러 안내 | 하나가 아예 빠진 경우만 막는다. accessToken= (빈 값), 공백, 문자열 "null"/"undefined"는 그대로 저장되고 머리글이 "마이페이지"(로그인 상태)로 바뀐다 | /oauth/callback?accessToken=&refreshToken=b 등 직접 진입 | src/pages/OAuthCallbackPage.tsx:18(=== null만 검사) · src/utils/auth.ts:109-111(isLoggedIn이 "" 도 로그인으로 봄) | 확인: localStorage 값과 머리글. 영향은 제한적 — 잘못된 토큰으로 첫 인증 요청을 보내면 401을 받아 client.ts가 스스로 로그아웃시킨다. (첫 판에 "지시서 0-4의 백엔드 "null" 문자열 결함"과 연결해 적었는데 틀렸다. 그 결함은 소셜 응답의 생년월일 값이다: KakaoResponse.java:41·46·51) |
| A-2 | AI 답 자리에 빈 말풍선이 생기지 않는다 | answer가 ""·null·공백이면 회색 빈 말풍선이 남는다(다시 시도·안내 없음) | 토큰을 넣고 /ai에서 질문, POST /api/open-ai/ask를 {answer:""}로 가로챔 | src/pages/AiPage.tsx:118-124(result.answer를 검사 없이 말풍선으로) | 확인: 스크린샷 shot-A2-answer"".png |
| H-3 | 카드 "불러오지 못함"과 "값 없음"이 화면에서 구분된다 | 카드 요청 실패(500×2)와 일봉 0개가 글자·모양까지 똑같다("– 한 달 전 대비", 빈 그래프 자리). 로딩 중 자리표시와도 같다 | / 에서 GET /api/stocks/000660을 500 또는 dailyPrices:[]로 가로챔 | src/api/home.ts:57-67(두 경우를 같은 catch로 삼키고 카드 값을 비워 둠) · src/components/TopStockCard.tsx:28,33-43(비어 있으면 로딩 자리표시를 그림) | 확인: 코드 경로 |
| H-상태-빈배열 | 목록이 비면 "종목이 없다"류 안내 | 머리글 줄만 남고 안내 없음 | / 에서 GET /api/stocks를 stockList:[]로 가로챔 | src/components/StockTable.tsx:100(빈 목록 분기 없이 map) | 확인 |
| H-상태-빈문자열 | 기준일이 빈 문자열이어도 표시가 깨지지 않는다 | "년 0월 0일 종가 기준" | / 에서 baseDate:"" | src/api/stock.ts:32 → src/pages/HomePage.tsx:188 → src/utils/date.ts:38-45(toKoreanDate가 빈 연도를 올해가 아니라고 보고 "년 "을 붙이고 Number("")=0) | 확인: 단위 검사 toKoreanDate("")="년 0월 0일" |
| S-3 | 차트 실패 안내가 400px 박스 가운데 | 박스째 사라지고 "종목 정보를 불러오지 못했습니다"가 (120,140)에 왼쪽 정렬. 높이 300px 넘는 감싸는 박스 없음(정상 차트 박스는 340px) | /stocks/005930, 상세 GET을 500으로 가로챔 | src/pages/StockChartPage.tsx:148-160(실패 시 가격·차트 구역 전체를 제목+버튼으로 교체) | 확인. 상세 요청 하나가 가격·차트를 함께 주는 구조라 차트만 따로 실패하는 상태는 없다 |
| S-4 | 뉴스 실패 시 담는 박스가 유지된 채 안내 | "뉴스를 불러오지 못했습니다." 글자만 섹션에 떠 있다(배경·테두리 있는 조상 없음). 다시 시도 수단도 없다 | /stocks/005930, 뉴스 GET을 500으로 | src/components/NewsList.tsx:113-115 | 확인 |
| S-뉴스-더보기실패 | 더 보기 실패 → 받아 둔 목록 유지 + 실패 안내 + 다시 누를 수단 | 목록 10건과 "뉴스 더 보기" 버튼은 남지만 실패했다는 표시가 없다(눌러도 아무 일 없던 것처럼 보임) | /stocks/005930, page=1만 500 | src/components/NewsList.tsx:75-77(console.warn만) | 확인 |
| S-뉴스-제목빈문자열 | 제목·링크가 빈 기사가 빈 링크 카드가 되지 않는다 | 제목 줄 없는 카드가 href="" target=_blank로 그려진다 — 누르면 지금 보던 종목 페이지가 새 탭에 열린다 | 뉴스 첫 기사를 title:"", originalLink:""로 | src/components/NewsList.tsx:128(href={item.link}) · src/api/stock.ts:154-163(toNewsItem이 빈 값을 거르지 않음) | 확인 |
| P-4 | 설명·이미지가 빈 값이면 프론트 문구·이미지로 대신 | ""·null은 대신한다. 공백만 있는 값("  ")은 대신하지 않아 설명이 통째로 빠지고 이미지가 src=" "로 깨진다 | /mypage/personality에서 description:"  ", personalityImg:" " | src/pages/MypagePersonalityPage.tsx:82-83 · src/api/personality.ts:106-107(\|\| 연산은 공백 문자열을 참으로 봄) | 확인 |
| P-상태-보기빈배열 | 보기가 빈 문항이 와도 진행 불가 상태에 갇히지 않는다 | 보기 0개, "다음"은 잠김, 안내 없음 — 더 나아갈 수 없다 | GET /api/personality-tests의 첫 문항 choices를 []로 | src/api/personality.ts:61-65(문항 수만 검사) · src/pages/PersonalityTestPage.tsx:115 | 확인 |
| M-상태-해제실패 | 관심종목 해제 실패 → 목록에 되돌리고 알린다 | 되돌리기는 한다(1→0→1줄). 알리는 문구·role=alert가 없어 누르기 전과 화면이 완전히 같다. 홈 하트도 같다 | /mypage/likes, DELETE를 500으로 | src/pages/MypageLikesPage.tsx:66-69 · src/pages/HomePage.tsx:146-148 | 확인 |
| C-3 | 키보드 포커스 표시가 보인다 | /ai 질문 입력칸만 포커스 전후 화면이 픽셀까지 같다. 나머지 화면·입력은 표시 있음(홈 검색칸 등은 감싸는 상자 테두리로 표시) | /ai에서 Tab으로 질문칸에 들어가 전후 스크린샷 비교 | src/pages/AiPage.module.css:127-129(.input:focus { outline: none }, 감싸는 상자에 :focus-within 표시 없음) | 확인 |
| W-R-2 | 한 영역 실패가 화면 전체를 내리지 않는다 | 화면 안쪽 경계가 없어 한 구역 오류가 화면 전체를 내린다(실측 4곳) | B-상태-null, M-상태-null, 추가 발견 1 | src/App.tsx:69-111 | 확인 |
| W-API-4 | 실패가 하나의 에러 모양으로 모인다 | ApiError / Error / TypeError 세 갈래. 의도 밖: HTTP 200 빈 본문이 TypeError, 래퍼 없는 200이 code undefined | 개발 서버에서 client.get을 직접 호출 | src/api/client.ts:200-210, 231-235 | 확인 |
| W-API-6 | 프론트 타입이 스웨거와 같다 | 이름·자료형은 같고, 필수 여부가 다르다(스웨거 응답 필드는 전부 선택) | api-docs.json 대조 | src/types/*.ts, src/api/stock.ts:24-51 | 확인 |
| W-FN-1 | 범위 밖 점수는 서버처럼 결과가 없다 | 9·0→안정형, 91·NaN→공격투자형 | 단위 검사 | src/utils/personality.ts:54-60 | 확인. 실제 문항으로는 범위 밖 합이 나오지 않아 영향은 보기 빈 문항(NaN)뿐 |
| W-FN-5 | undefined·문자열·NaN을 받아도 화면용 글자를 낸다 | undefined → formatChangeRate·formatPrice·formatBirth가 TypeError, toPercent(null)→"0.00%", NaN→"NaN%" 등 | 단위 검사 | src/utils/format.ts:12,17,27,68 · src/utils/backtest.ts:184-186 | 확인. 화면을 실제로 죽이는 경로는 추가 발견 1 |
| W-FN-7 | 설명 "", " ", null을 모두 대신한다 | " "만 못 대신함 | 단위 검사 | src/api/personality.ts:106-107 | 확인 |
| W-R-4 | 로딩/실패/빈 값을 모두 다룬다 | X 11곳(아래 표) | 블랙박스 종합 | 각 행 참조 | 확인 |

### W-R-4 표 (로딩 / 실패 / 빈 값)
| 컴포넌트 | 로딩 | 실패 | 빈 값 |
|---|---|---|---|
| 홈 시세표 | O 불러오는 중… | O 불러오지 못했습니다 + 다시 시도 | X 머리글만(H-상태-빈배열) |
| 홈 테마 카드 | O 자리표시 | X 자리표시 그대로(H-3) — 3장 모두 실패면 O | X 자리표시 그대로(H-3) |
| 종목 상세 | O 스켈레톤(가격 자리는 "-") | O 불러오지 못했습니다 + 다시 시도 | O 이 기간에는 거래일이 없습니다 |
| 뉴스 | O 불러오는 중… | O 문구(박스·다시 시도 없음, S-4) / X 더 보기 실패 | O 관련 뉴스를 찾지 못했습니다 |
| 성향검사 문항 | O 불러오는 중… | O 예비 7문항(알림 없음) | O 예비 7문항 / X 보기 빈 문항 |
| 성향검사 저장 | — (버튼 잠금) | X 문항 실패 문구로 바뀜 | — |
| 백테스트 결과 | O 불러오는 중… + 버튼 잠금 | O 코드별 문구 + 다시 시도 | X result null이면 화면 전체 오류 |
| AI 답 | O 답변을 생성하고 있어요 + 입력 잠금 | O 답변을 가져오지 못했어요 + 다시 시도 | X 빈 말풍선 |
| 내 정보 | O 스켈레톤 | O 문구 + 다시 시도 | X result null이면 화면 전체 오류 |
| 투자유형 | (재지 않음) | O 문구 + 다시 시도 | X 404(성향 없음)를 실패로 / O null은 검사 안내 |
| 관심종목 | (재지 않음) | O 문구 + 다시 시도 / X 해제 실패 알림 없음 | O 아직 관심종목이 없어요 |

## 전체
| ID | 판정 | 예상 | 실제 | 일부러 실패시켜 봤나 | 근거(파일:줄 / 스크린샷) |
|---|---|---|---|---|---|
| D-1 | 문서와 다름 | 지시서: 대상 저장소 JUBYInvest/JUBY-FE | 이 검사는 yeopkwang/juby-fe-personal(개인 저장소)에서 했다. 브랜치 이름은 같다(feat/login-and-stock-detail) | 해당 없음 | git remote -v |
| D-2 | 문서와 다름 | 지시서: home.ts·candles.ts·quoteSnapshot.ts(0-4), personality.html(2-3·W-B-3) | candles.ts·quoteSnapshot.ts·personality.html은 없다. 성향검사 주소는 /personality-test | 해당 없음 | ls |
| D-3 | 문서와 다름 | 지시서: 커밋 0cf23ba(B-6)·ba536df(W-D-2)·e7baca9(W-S-2) | 세 커밋 모두 이 브랜치의 조상이 아니다. origin/feat/backtest-strategy-cards에만 있다 | 해당 없음 | git merge-base --is-ancestor |
| D-4 | 문서와 다름 | 지시서: 잠금 세 곳(API_DISABLED·LoginPage 소셜 이동·vite 프록시) | API_DISABLED=false(2026-09-18 해제), LoginPage는 막지 않고 바로 이동(L-1), vite 프록시는 열려 있음. 지시서대로 어느 것도 바꾸지 않았다 | 해당 없음 | client.ts:36, LoginPage.tsx:54, vite.config.ts:13-15 |
| D-5 | 문서와 다름 | 지시서: "홈 한 번이 108건"(0-1), 102종목 현재가 5개씩 120ms(H-1) | 홈은 GET /api/stocks 1건 + 카드 3건(순차, 약 200ms 간격) | 해당 없음 | H-1 |
| D-6 | 문서와 다름 | 지시서: NewsList.tsx:59 "수정 작업 A", AiPage.tsx:120 "수정 작업 B", "수정 작업 C 스크립트" | 두 줄 번호는 지금 각각 loadMore 선언부와 답 말풍선 조립 코드다. 작업 A·B·C라는 이름의 변경을 이 저장소에서 찾을 수 없어 해당 항목은 동작으로만 판정했다(S-5, A-3, W-S-1) | 해당 없음 | sed |
| D-7 | 문서와 다름 | 지시서: AI 질문이 GET /api/open-ai/ask, 답은 아직 안 온다(A-1·A-2) | POST이고 실제 답을 받는다 | 해당 없음 | A-1 |
| D-8 | 문서와 다름 | 지시서: 성향 POST를 끄는 플래그(P-7), 백엔드가 인증 실패를 500으로 준다(M-3) | 플래그 없이 로그인 여부로 갈린다. CLAUDE.md는 백엔드가 이제 제대로 401을 준다고 적고 있다 | 해당 없음 | P-7, M-3 |
| H-1 | 문서와 다름 | 102종목 현재가 요청이 5개씩 약 120ms 간격으로 나뉘어 나간다. 한꺼번에 몰리지 않는다 | 홈 한 번에 /api 요청 4건: 목록 GET /api/stocks 1건 + 카드 GET /api/stocks/{code}?period=ONE_MONTH 3건. 102종목 현재가 요청 자체가 없다. 카드 3건은 순차(응답 500ms 지연 시 시작 간격 707ms, 705ms → 앞 응답 뒤 약 207/205ms 쉬고 다음 요청). 순차 여부: 예 | "동시 발사" 판정을 대면 거짓 → 판정 유효 | Playwright 요청 기록(운영 빌드) |
| H-2 | 문서와 다름 | 일부 종목만 500 → 실패한 행만 비고 나머지는 채워진다. 표 전체가 깨지지 않는다 | 행별 현재가 요청이 없어 "일부 행만 실패"는 생길 수 없다(표는 요청 1건). 대응 상황인 카드 1장(012450)만 500: 요청 2회(재시도 1회 포함), 카드 [기술주 대장 SK하이닉스 +0.1% 한 달 전 대비 \| 방산주 대장 한화에어로스페이스 – 한 달 전 대비 \| 바이오주 대장 삼성바이오로직스 +0.4% 한 달 전 대비], 표 5줄 유지. 나머지 카드는 채워짐: 예 | 실패를 빼면 판정이 거짓 → 유효 | 카드 innerText 비교 |
| H-3 | 실패 | "불러오지 못함"과 "값 없음"이 화면에서 구분된다 | 카드 실패(500×2): "기술주 대장 SK하이닉스 – 한 달 전 대비" / 카드 값 없음(일봉 0개): "기술주 대장 SK하이닉스 – 한 달 전 대비" → 글자가 똑같아 구분 불가. 표는 행 단위 실패가 없고, 값이 null인 행은 "005930 삼성전자 - - -" | 해당 없음(실패) | shot-H3-card-fail.png, shot-H3-card-empty.png · src/api/home.ts:57-67, src/components/TopStockCard.tsx:28,33-43 |
| H-4 | 사람 확인 | 첫 열 머리글과 실제 값이 같은 의미다 | 머리글 "현재 주가 보기 9월 22일 종가 기준" 중 첫 값 열 이름은 "번호", 그 열 값은 005930, 000660, 373220…(모두 6자리 종목코드: 예). 이름과 값의 뜻이 다르다. 단, 이전 대화에서 "번호" 표기는 의도한 것이라고 한 항목이라 판정을 사람에게 넘긴다 | 해당 없음 | DOM 텍스트 |
| H-5 | 문서와 다름 | 장 시작 전 응답(등락률·거래량 0) → 저장된 지난 장 값으로 대신 보여주고, 그 사실을 알 수 있는 표시가 있다 | "지난 장 값으로 대신 보여주기"는 없다 — 목록이 현재가가 아니라 DB 종가라 장 시작 전 0이 오는 구조가 아니다. 0을 주면 그대로 "005930 삼성전자 71,200원 0.00% 0만원". 대신 표 위에 "9월 22일 종가 기준" 표시가 있어 언제 값인지 알 수 있다(표시가 충분한지는 사람 확인) | 해당 없음 | shot-H5-zero.png |
| H-6 | 통과 | 테마 카드 스파크라인 실패 → 카드가 무너지지 않고 빈 상태로 남는다 | 실패 카드 331×289px(옆 카드 331×289px), 그래프 선 0개, 글자 "방산주 대장 한화에어로스페이스 – 한 달 전 대비" | 예 — 정상 응답이면 판정이 거짓 → 유효 | shot-H6-card-fail.png |
| H-7 | 통과 | 행 클릭 → /stocks/:stockCode 로 올바른 코드가 넘어간다 | 4번째 행(코드 035420) 종목명 링크 클릭 → /stocks/035420, 제목 "NAVER 035420". 같은 행의 빈 칸(거래대금) 클릭 → /stocks/035420 (종목명 링크가 행 전체를 덮어 어디를 눌러도 이동) | 예 — 다른 코드를 기대하면 거짓 → 유효 | URL |
| H-상태-느림 | 통과 | 2.5초 지연 → 로딩 중임을 알린다 | 1초 시점 표 구역 "현재 주가 보기 시세를 불러오는 중…" · 행 0개 · 로딩 표시 요소 1개 → 3.5초 시점 행 5개 | 예 — 정상 응답 화면엔 같은 문구가 없어 판정이 거짓 → 유효 | shot-H-상태-느림.png |
| H-상태-500 | 통과 | 500 → 표 자리에 실패 안내, 화면은 살아 있다 | 표 구역 "현재 주가 보기 시세를 불러오지 못했습니다. 다시 시도" · 행 0개 · 오류화면 안 뜸 · 목록 요청 1회 | 예 — 정상 응답에 같은 판정을 대면 거짓 → 유효 | shot-H-상태-500.png |
| H-상태-끊김 | 통과 | 네트워크 끊김 → 실패 안내, 화면은 살아 있다 | 표 구역 "현재 주가 보기 시세를 불러오지 못했습니다. 다시 시도" · 행 0개 · 오류화면 안 뜸 · 목록 요청 1회 | 예 — 정상 응답에 같은 판정을 대면 거짓 → 유효 | shot-H-상태-끊김.png |
| H-상태-null | 통과 | result: null → 화면이 죽지 않고 빈/실패 안내 | 표 구역 "현재 주가 보기 시세를 불러오지 못했습니다. 다시 시도" · 행 0개 · 오류화면 안 뜸 · 목록 요청 1회 | 예 — 정상 응답에 같은 판정을 대면 거짓 → 유효 | shot-H-상태-null.png |
| H-상태-isSuccess거짓 | 통과 | HTTP 200 + isSuccess:false → 성공으로 새지 않는다 | 표 구역 "현재 주가 보기 시세를 불러오지 못했습니다. 다시 시도" · 행 0개 · 오류화면 안 뜸 · 목록 요청 1회 | 예 — 정상 응답에 같은 판정을 대면 거짓 → 유효 | shot-H-상태-isSuccess거짓.png |
| H-상태-HTML본문 | 통과 | 502 + HTML 본문(게이트웨이) → 실패 안내 | 표 구역 "현재 주가 보기 시세를 불러오지 못했습니다. 다시 시도" · 행 0개 · 오류화면 안 뜸 · 목록 요청 1회 | 예 — 정상 응답에 같은 판정을 대면 거짓 → 유효 | shot-H-상태-HTML본문.png |
| H-상태-빈배열 | 실패 | stockList: [] → "종목이 없다"류 안내 | 표 구역 "현재 주가 보기 9월 22일 종가 기준 번호 종목명 종가 등락률 거래대금" · 행 0개 · 오류화면 안 뜸 · 목록 요청 1회 | 해당 없음(실패) | shot-H-상태-빈배열.png |
| H-상태-빈문자열 | 실패 | baseDate: "" → 기준일 표시가 깨지지 않는다(NaN·Invalid·0월 0일 없음) | 표 구역 "현재 주가 보기 년 0월 0일 종가 기준 번호 종목명 종가 등락률 거래대금 005930 삼성전자 71,200원 +1.28% 1.3조 000660 SK하이닉스 198,000원 -2.41% 8,200억 373220 LG에너지솔루션 402,000원 +0.50% 3,100억 035420 NAVER 188,000원 -0.37% 1,200억 005380 현대차 243" · 행 5개 · 오류화면 안 뜸 · 목록 요청 1회 | 해당 없음(실패) | shot-H-상태-빈문자열.png |
| H-상태-다시시도 | 통과 | 실패 후 "다시 시도"를 누르면 다시 요청해 표가 채워진다 | 누르기 전 행 0개 → 누른 뒤 행 5개, 목록 요청 2회 | 예 — "누르기 전에도 5행" 판정을 대면 거짓 → 유효 | DOM |
| S-1 | 통과 | 존재하지 않는 코드(/stocks/000000) → 빈 차트가 아니라 "없는 종목" 안내 | 404 응답 → "목록에 없는 종목입니다 홈으로 돌아가기" (차트 0개, 상세 요청 1회: 4xx는 재시도 안 함). 참고: 같은 코드에 서버가 500을 주면 "종목 정보를 불러오지 못했습니다 다시 시도 홈으로 돌아가기"(요청 2회). 형식이 틀린 /stocks/abc는 "종목 정보를 불러오지 못했습니다 다시 시도 홈으로 돌아가기" · 나간 요청 [/api/stocks/abc/news,/api/stocks/abc,/api/stocks/abc] | 예 — 정상 응답에 판정을 대면 거짓 → 유효 | shot-S1.png |
| S-2 | 통과 | 차트 느림(2.5초) → 로딩 표시가 있다. 같은 요청이 두 번 나가지 않는다 | 1초 시점 차트 자리에 스켈레톤 1개(글자 없음), 다 뜬 뒤 0개. 상세 요청 1회(운영 빌드 기준, 기간 탭 7개를 눌러도 1회). 참고: 로딩 중 가격 자리는 "-"로, 값이 없을 때(S-6)와 같은 모양이다. 보조기기용 로딩 표시(aria-busy/role=status) 0개 | 예 — 다 뜬 화면에 같은 판정을 대면 스켈레톤 0개라 거짓 → 유효 | shot-S2-loading.png |
| S-3 | 실패 | 차트 실패 → 안내 문구가 400px 박스 가운데에 온다 | 안내 "종목 정보를 불러오지 못했습니다"가 (120, 140)에 1040×40px로 왼쪽 정렬. 높이 300px 이상인 감싸는 박스: 없음. 상세 요청 하나가 가격·차트를 함께 주므로 실패하면 차트 박스(정상 시 340px)째 사라지고 가격 구역 전체가 안내로 바뀐다. 뉴스 구역: "(뉴스 구역 없음)" | 해당 없음(실패) | shot-S3.png |
| S-4 | 실패 | 뉴스 실패 → 담는 박스가 유지된 채 안내가 나온다 | 뉴스 500 → 안내 "뉴스를 불러오지 못했습니다.", 안내를 감싸는 박스(배경·테두리 있는 조상): 없음 — 글자만 섹션에 떠 있다 | 해당 없음(실패) | shot-S4.png |
| S-5 | 통과 | 뉴스 result: null → 화면 전체가 ErrorBoundary로 넘어가지 않고 뉴스 영역만 실패 처리 | 오류화면 안 뜸, 차트 1개 유지, 뉴스 구역 "뉴스 모아보기 최신순 관련도순 뉴스를 불러오지 못했습니다." | 예 — 정상 뉴스에 판정을 대면 거짓 → 유효 | DOM |
| S-6 | 사람 확인 | 값 없음 표시의 큰 대시(—)가 구분선처럼 보이지 않는다 | 현재가·등락률 null, 일봉 0개 → "‹ 홈으로 돌아가기 삼성전자 005930 - 전일 대비 - 1주 1개월 3개월 6개월 1년 3년 전체 이 기간에는 거래일이 없습니다." (차트 1개, 오류화면 안 뜸) | 해당 없음 | shot-S6-dash.png |
| S-7 | 문서와 다름 | 장 시작 전 0.00% → 홈과 같은 규칙(지난 장 값 대체)이 적용된다 | 홈에도 상세에도 "지난 장 값 대체" 규칙이 없다(H-5 참고). 등락률 0이 오면 그대로 "전일 대비 0.00%" | 해당 없음 | DOM |
| S-8 | 문서와 다름 | 목록 → 상세 이동 시 목록에서 미리 받은 일봉을 다시 요청하지 않는다 | 목록(표)은 일봉을 받지 않는다(GET /api/stocks 한 건에 종가만). 홈 카드만 ONE_MONTH 일봉을 받는데, 카드 → 상세로 가면 /api/stocks/000660?period=ALL를 새로 받는다(기간이 ALL이라 재사용 불가). 표에서 같은 종목 상세를 두 번 오가면 상세 요청이 매번 나간다 — 전체 순서: /api/stocks/000660?period=ONE_MONTH → /api/stocks/012450?period=ONE_MONTH → /api/stocks/207940?period=ONE_MONTH → /api/stocks/000660?period=ALL → /api/stocks/000660?period=ONE_MONTH → /api/stocks/012450?period=ONE_MONTH → /api/stocks/207940?period=ONE_MONTH → /api/stocks/005930?period=ALL → /api/stocks/000660?period=ONE_MONTH → /api/stocks/012450?period=ONE_MONTH → /api/stocks/207940?period=ONE_MONTH → /api/stocks/005930?period=ALL | 해당 없음 | Playwright 요청 기록 |
| S-상태-500 | 통과 | 500 → 한 번 재시도 후 실패 안내 | 위 구역 "종목 정보를 불러오지 못했습니다 다시 시도 홈으로 돌아가기" · 차트 0개 · 오류화면 안 뜸 · 상세 요청 2회 · 탭 제목 "종목 - JUBY" | 예 — 정상 응답에 판정을 대면 거짓 → 유효 | shot-S-상태-500.png |
| S-상태-끊김 | 통과 | 끊김 → 실패 안내 | 위 구역 "종목 정보를 불러오지 못했습니다 다시 시도 홈으로 돌아가기" · 차트 0개 · 오류화면 안 뜸 · 상세 요청 2회 · 탭 제목 "종목 - JUBY" | 예 — 정상 응답에 판정을 대면 거짓 → 유효 | shot-S-상태-끊김.png |
| S-상태-null | 통과 | result: null → 실패 안내(오류 화면 아님) | 위 구역 "종목 정보를 불러오지 못했습니다 다시 시도 홈으로 돌아가기" · 차트 0개 · 오류화면 안 뜸 · 상세 요청 2회 · 탭 제목 "종목 - JUBY" | 예 — 정상 응답에 판정을 대면 거짓 → 유효 | shot-S-상태-null.png |
| S-상태-isSuccess거짓 | 통과 | 200 + isSuccess:false → 실패 안내 | 위 구역 "종목 정보를 불러오지 못했습니다 다시 시도 홈으로 돌아가기" · 차트 0개 · 오류화면 안 뜸 · 상세 요청 2회 · 탭 제목 "종목 - JUBY" | 예 — 정상 응답에 판정을 대면 거짓 → 유효 | shot-S-상태-isSuccess거짓.png |
| S-상태-일봉빈배열 | 통과 | dailyPrices: [] → 차트 자리에 "데이터 없음"류 안내, 화면은 살아 있다 | 위 구역 "‹ 홈으로 돌아가기 삼성전자 005930 71,200원 전일 대비 +1.28% 1주 1개월 3개월 6개월 1년 3년 전체 이 기간에는 거래일이 없습니다." · 차트 1개 · 오류화면 안 뜸 · 상세 요청 1회 · 탭 제목 "삼성전자 - JUBY" | 예 — 정상 응답에 판정을 대면 거짓 → 유효 | shot-S-상태-일봉빈배열.png |
| S-상태-일봉null | 통과 | dailyPrices: null → 화면이 죽지 않는다 | 위 구역 "종목 정보를 불러오지 못했습니다 다시 시도 홈으로 돌아가기" · 차트 0개 · 오류화면 안 뜸 · 상세 요청 2회 · 탭 제목 "종목 - JUBY" | 예 — 오류화면 감지 판정식은 B-상태-null·M-상태-null에서 거짓을 냈다 → 유효 (이 항목의 판정식은 "화면이 죽지 않는다"만 본다) | shot-S-상태-일봉null.png |
| S-상태-종목명빈문자열 | 통과 | stockName: "" → 제목 자리가 비지 않는다(코드 등으로 대신) | 위 구역 "‹ 홈으로 돌아가기 005930 71,200원 전일 대비 +1.28% 1주 1개월 3개월 6개월 1년 3년 전체 9월 22일 시 71,200 고 71,500 저 70,400 종 70,700 +0.14% 거래량 1,000만주 9월 22일 마감 기준 시가 71,200원 고가 71,500원 " · 차트 1개 · 오류화면 안 뜸 · 상세 요청 1회 · 탭 제목 "JUBY" | 예 — 오류화면 감지 판정식은 B-상태-null·M-상태-null에서 거짓을 냈다 → 유효 (이 항목의 판정식은 "화면이 죽지 않는다"만 본다) | shot-S-상태-종목명빈문자열.png |
| S-상태-다시시도 | 통과 | 실패 후 "다시 시도" → 다시 요청해 차트가 뜬다 | 누르기 전 차트 0개 → 누른 뒤 1개, 상세 요청 총 3회 | 예 — "누르기 전에도 차트" 판정은 거짓 → 유효 | DOM |
| S-뉴스-느림 | 통과 | 뉴스 2.5초 지연 → 뉴스 자리에 로딩 표시, 차트는 먼저 뜬다 | 뉴스 구역 "뉴스 모아보기 최신순 관련도순 불러오는 중…" · 차트 1개 · 오류화면 안 뜸 · 뉴스 요청 1회 | 예 — 정상 뉴스에 판정을 대면 거짓 → 유효 | shot-S-뉴스-느림.png |
| S-뉴스-끊김 | 통과 | 뉴스 끊김 → 뉴스만 실패 안내 | 뉴스 구역 "뉴스 모아보기 최신순 관련도순 뉴스를 불러오지 못했습니다." · 차트 1개 · 오류화면 안 뜸 · 뉴스 요청 1회 | 예 — 정상 뉴스에 판정을 대면 거짓 → 유효 | shot-S-뉴스-끊김.png |
| S-뉴스-빈배열 | 통과 | newsList: [] → "뉴스가 없다"류 안내 | 빈 배열 → "뉴스 모아보기 최신순 관련도순 관련 뉴스를 찾지 못했습니다." / 실패(500) → "뉴스 모아보기 최신순 관련도순 뉴스를 불러오지 못했습니다." → 빈 것과 실패가 다른 문구로 구분된다 | 예 — 정상 뉴스·실패 응답에 같은 판정을 대면 둘 다 거짓 → 유효 (첫 실행은 탐지 정규식이 "없"만 찾아 오판했다) | DOM |
| S-뉴스-newsList없음 | 통과 | newsList 필드 자체가 없음 → 뉴스만 실패/빈 안내 | 뉴스 구역 "뉴스 모아보기 최신순 관련도순 뉴스를 불러오지 못했습니다." · 차트 1개 · 오류화면 안 뜸 · 뉴스 요청 1회 | 예 — 정상 뉴스에 판정을 대면 거짓 → 유효 | shot-S-뉴스-newsList없음.png |
| S-뉴스-제목빈문자열 | 실패 | 뉴스 제목 "" → 카드가 깨지지 않는다(빈 링크 없음) | 제목·링크가 빈 기사도 카드로 그려진다: 글자 "출처 미상·1시간 전 테스트 뉴스 요약입니다.", href=""(브라우저 해석 http://localhost:4173/stocks/005930), target=_blank → 누르면 지금 보는 종목 페이지가 새 탭에 열린다. 제목 줄은 비어 있다 | 정상 기사는 href="https://example.com/news/1"라 "빈 링크" 판정 거짓 → 유효 | DOM |
| S-뉴스-더보기실패 | 실패 | "뉴스 더 보기"(page=1) 실패 → 받아 둔 10건은 그대로, 실패 안내와 다시 누를 수단이 있다 | 카드 10건 유지 · 구역 끝 " 뉴스 제목 8 테스트 뉴스 요약입니다. example.com·9시간 전 테스트 뉴스 제목 9 테스트 뉴스 요약입니다. example.com·10시간 전 테스트 뉴스 제목 10 테스트 뉴스 요약입니다. 뉴스 더 보기" · 구역 안 버튼 [최신순, 관련도순, 뉴스 더 보기] | 해당 없음(실패) | shot-S-뉴스-더보기실패.png |
| H-카드-null | 통과 | 홈 카드 응답 result: null → 화면이 죽지 않고 그 카드만 비어 있다 | 카드 "기술주 대장 SK하이닉스 – 한 달 전 대비", 오류화면 안 뜸 | 예 — 정상 카드엔 등락률이 있어 "빈 카드" 판정 거짓 → 유효 | DOM |
| P-1 | 통과 | 실제 GET /api/personality-tests → 문항 10개 × 보기 5개가 그려진다 | 실제 응답 문항 10개. 화면에서 넘기며 센 보기 수 [1 / 10:5 2 / 10:5 3 / 10:5 4 / 10:5 5 / 10:5 6 / 10:5 7 / 10:5 8 / 10:5 9 / 10:5 10 / 10:5]. 실제 호출 1회 | 예 — "보기 4개" 기대를 대면 거짓 → 유효 | personality-real.json |
| P-2 | 통과 | 모든 문항 최저점 / 최고점 → 합계 10 / 90, 서버 채점 구간과 같은 결과명 | 합 10 → 화면 "안정형"(서버 기준 안정형) at /personality-test/result / 합 90 → 화면 "공격투자형"(서버 기준 공격투자형) at /personality-test/result. 합계 점수 자체는 화면에 안 보인다. 화면 글: "나의 투자성향 테스트 당신의 투자성향은? 안정형 원금 손실을 무엇보다 먼저 피하며 예금·적금 수준의 안정적인 수익을 기대하는 스타일 완료 검사 다시하기" | 예 — 한 구간 옆의 결과를 기대하면 거짓 → 유효 | shot-P2-1.png, shot-P2-9.png |
| P-3 | 통과 | 구간 경계값(각 구간 시작·끝 점수) → 경계에서 결과가 어긋나지 않는다 | 14→안정형, 16→안정추구형, 34→안정추구형, 36→위험중립형, 54→위험중립형, 56→적극투자형, 74→적극투자형, 76→공격투자형. 보기 점수가 모두 홀수라 10문항 합은 짝수만 나와 15·35·55·75 자체는 화면으로 못 만든다(함수 단위 W-FN-1에서 확인) | 예 — 경계를 한 칸 옮긴 채점표를 대면 어긋남 → 유효 | DOM |
| P-4 | 실패 | 결과 설명·이미지가 빈 문자열 → 프론트 자체 문구·이미지로 대신한다 | (가) 성향검사 결과(POST 응답 빈 값): "나의 투자성향 테스트 당신의 투자성향은? 위험중립형 안정성과 수익성 사이에서 균형을 추구하며 시장 평균 수준의 위험을 감수하는 스타일 완료 검사 다시하기" 이미지 [{"src":"/personality/neutral.svg","ok":true,"alt":""}] / (나) 마이페이지(GET 빈 값): "마이페이지 마이페이지 투자유형 보기 관심종목 내 정보 확인 로그아웃 당신의 투자성향은? 위험중립형 안정성과 수익성 사이에서 균형을 추구하며 시장 평균 수준의 위험을 감수하는 스타일 검사 다시하기" 이미지 [{"src":"/personality/neutral.svg","ok":true,"alt":""}] / (다) 공백만: "마이페이지 마이페이지 투자유형 보기 관심종목 내 정보 확인 로그아웃 당신의 투자성향은? 위험중립형 검사 다시하기" 이미지 [{"src":" ","ok":false,"alt":""}] / 기준(서버 설명 있음): "마이페이지 마이페이지 투자유형 보기 관심종목 내 정보 확인 로그아웃 당신의 투자성향은? 위험중립형 서버가 준 설명 검사 다시하기" → 빈 문자열은 프론트 문구·이미지로 대신하지만, 공백만 있는 값은 빈 값으로 보지 않아 설명이 통째로 빠지고 이미지 src=" "로 깨진다 | 빈 문자열 두 경우는 대체됨을 확인, 공백 경우만 실패 | shot-P4-result.png, shot-P4-mypage.png |
| P-5 | 사람 확인 | 문항 중간에 새로고침·뒤로가기 → 동작을 기록 (정답 없음) | 5 / 10에서 새로고침 → 1 / 10(답 모두 사라지고 처음부터). 4 / 10에서 브라우저 뒤로가기 → / (문항 단위가 아니라 이전 페이지로 나가며, 경고 없이 답이 사라진다) | 해당 없음 | DOM |
| P-6 | 통과 | 답하지 않은 문항이 있을 때 제출 → 제출이 막히고 어느 문항인지 알려준다 | 한 화면에 한 문항씩이라 답하지 않은 문항 = 지금 보는 문항이다. Q1에서 다음 버튼 잠김: true. 10 / 10(마지막)에서 "결과 보기" 잠김: true, 강제로 눌러도 /personality-test에 머묾. /personality-test/result를 직접 열면 /personality-test로 되돌림. "이전"으로 가면 고른 답 [채권형 펀드, 원금보장형 ELS 등 저위험 상품]이 남아 있음. 잠긴 이유를 글로 알려주지는 않는다 | 예 — "마지막 문항에서 버튼이 열려 있다" 판정은 거짓 → 유효 | DOM |
| P-7 | 문서와 다름 | POST /api/personality-tests → 플래그가 꺼져 있어 요청이 나가지 않는다 (가로채기로 0건 확인) | 끄고 켜는 플래그가 없다. 로그인 여부로 갈린다 — 비로그인 POST 0건(프론트가 채점), 로그인 POST 1건 본문 {"scores":[9,9,9,7,1,1,1,1,1,1]}. (실제 서버로는 보내지 않고 가로챔) | 해당 없음 | Playwright 요청 기록 |
| P-상태-느림 | 통과 | 2.5초 지연 → 로딩 표시 | 본문 "불러오는 중…" · 진행 1 / 10 · 보기 5개 · 오류화면 안 뜸 | 예 — 정상 화면엔 "불러오는 중"이 없어 판정 거짓 → 유효 | shot-P-상태-느림.png |
| P-상태-500 | 통과 | 500 → 화면이 살아 있고 안내 또는 예비 문항 | 본문 "나의 투자성향 테스트 Q1. 1 / 7 나에게 1천만 원의 여유 자금이 생겼다. 1천만 원을 예금과 주식투자로 배분한다면? 예금 1,000만 원 예금 700만원, 주식 300만원 예금 500만원, 주식 500만원 예금 300만원, 주식 700만원 주식 1,000만 원 이전 다음" · 진행 1 / 7 · 보기 5개 · 오류화면 안 뜸 — 서버 문항 대신 예비 7문항으로 조용히 바뀐다(사용자에게 알리지 않음) | 예 — 정상 응답은 "1 / 10"이라 "예비 문항(1 / 7)" 판정이 거짓 → 유효 | shot-P-상태-500.png |
| P-상태-끊김 | 통과 | 끊김 → 안내 또는 예비 문항 | 본문 "나의 투자성향 테스트 Q1. 1 / 7 나에게 1천만 원의 여유 자금이 생겼다. 1천만 원을 예금과 주식투자로 배분한다면? 예금 1,000만 원 예금 700만원, 주식 300만원 예금 500만원, 주식 500만원 예금 300만원, 주식 700만원 주식 1,000만 원 이전 다음" · 진행 1 / 7 · 보기 5개 · 오류화면 안 뜸 — 서버 문항 대신 예비 7문항으로 조용히 바뀐다(사용자에게 알리지 않음) | 예 — 정상 응답은 "1 / 10"이라 "예비 문항(1 / 7)" 판정이 거짓 → 유효 | shot-P-상태-끊김.png |
| P-상태-null | 통과 | result: null → 안내 또는 예비 문항(오류 화면 아님) | 본문 "나의 투자성향 테스트 Q1. 1 / 7 나에게 1천만 원의 여유 자금이 생겼다. 1천만 원을 예금과 주식투자로 배분한다면? 예금 1,000만 원 예금 700만원, 주식 300만원 예금 500만원, 주식 500만원 예금 300만원, 주식 700만원 주식 1,000만 원 이전 다음" · 진행 1 / 7 · 보기 5개 · 오류화면 안 뜸 — 서버 문항 대신 예비 7문항으로 조용히 바뀐다(사용자에게 알리지 않음) | 예 — 정상 응답은 "1 / 10"이라 "예비 문항(1 / 7)" 판정이 거짓 → 유효 | shot-P-상태-null.png |
| P-상태-빈배열 | 통과 | questions: [] → 안내 또는 예비 문항 | 본문 "나의 투자성향 테스트 Q1. 1 / 7 나에게 1천만 원의 여유 자금이 생겼다. 1천만 원을 예금과 주식투자로 배분한다면? 예금 1,000만 원 예금 700만원, 주식 300만원 예금 500만원, 주식 500만원 예금 300만원, 주식 700만원 주식 1,000만 원 이전 다음" · 진행 1 / 7 · 보기 5개 · 오류화면 안 뜸 — 서버 문항 대신 예비 7문항으로 조용히 바뀐다(사용자에게 알리지 않음) | 예 — 정상 응답은 "1 / 10"이라 "예비 문항(1 / 7)" 판정이 거짓 → 유효 | shot-P-상태-빈배열.png |
| P-상태-보기빈배열 | 실패 | 어떤 문항의 choices: [] → 진행 불가 상태로 갇히지 않는다 | 본문 "나의 투자성향 테스트 Q1. 1 / 10 당신의 연령대는 어떻게 되십니까? 이전 다음" · 진행 1 / 10 · 보기 0개 · 오류화면 안 뜸 — 보기가 없어 고를 수 없고 "다음"은 잠겨 있어 더 나아갈 수 없다(안내 없음) | 해당 없음(실패) | shot-P-상태-보기빈배열.png |
| P-상태-저장실패 | 실패 | 로그인 상태에서 POST 500 → 결과는 보여주되 저장 안 됐음을 알린다 | 로그인 상태에서 10문항을 다 답하고 "결과 보기" → POST 500이나 끊김이면 결과 화면으로 가지 못하고 /personality-test에 "문항을 불러오지 못했습니다."만 남는다. 버튼·링크 0개, 답 10개는 사라짐. 저장 실패인데 문항 읽기 실패 문구가 뜬다. (POST 401은 전역 처리대로 토큰 삭제 후 /login) | 해당 없음(실패) | shot-P-저장실패.png, dbg-p.mjs |
| B-1 | 통과 | 실제 options 호출 → 성향별로 고를 수 있는 기간만 활성화된다 | RSI 역추세 전략(type 1): 화면 [1개월,3개월,6개월,1년] / 응답 [1개월,3개월,6개월,1년] · 볼린저 밴드 돌파 전략(type 2): 화면 [3개월,6개월,1년] / 응답 [3개월,6개월,1년] · 이동평균 교차 전략(type 3): 화면 [6개월,1년] / 응답 [6개월,1년] · MACD 추세추종 전략(type 4): 화면 [3개월,6개월,1년] / 응답 [3개월,6개월,1년] · 돌파 전략(type 5): 화면 [3개월,6개월,1년] / 응답 [3개월,6개월,1년] · 실제 호출 /api/backtest/preset/options. 고를 수 없는 기간은 잠그지 않고 목록에서 뺀다 | 예 — 다른 전략의 기간 목록과 대면 어긋남 → 유효 | Playwright |
| B-2 | 통과 | 응답 값이 화면 숫자와 그대로 일치한다. 프론트가 지어낸 값이 남아 있지 않다 | 지시서 조합: 005930 / 위험중립형(이동평균 교차) / 1년. 응답 finalScore 31.7025, totalReturn 0, annualReturn 0, mdd 0, sharpe 0, volatility 0, positionCount 0 → 화면 누적수익률 0.00% · 연평균수익률 0.00% · 최대낙폭 (MDD) 0.00% · 샤프비율) 0.00 · 변동성) 0.00% · 거래횟수 0회. 어긋난 항목: 없음. 축 점수(안정성·수익성·효율성·성장성 100점 환산)는 응답에 없고 프론트가 원시 지표로 계산해 보여준다: "안정성 비중 25% 100 수익성 비중 25% 0 효율성 비중 25% 0 성장성 비중 25% 27" | 예 — 이 조합은 값이 전부 0이라 단위 착오 변이를 걸 수 없어, 같은 판정식을 B-2-보조(0 아닌 값)에 돌려 "×100 안 한 값" 기대가 거짓이 되는 것을 확인 | shot-B2-type3.png |
| B-3 | 통과 | 한 번 누를 때 요청 6건. 기간이 1년일 때 같은 요청이 두 번 나가는지 기록 | 한 번 누르면 preset 6건: investType 순서 [1, 2, 3, 4, 5, 3]. 고른 전략(type 3) 요청이 비교용 5건과 별도로 한 번 더 나가 같은 요청이 2번이다(1년뿐 아니라 어느 기간이든 고른 전략이 겹침 — 6개월로도 확인은 아래 B-3-6개월) · 6개월·볼린저(type 2)로 누르면 [1/ONE_YEAR, 2/ONE_YEAR, 3/ONE_YEAR, 4/ONE_YEAR, 5/ONE_YEAR, 2/SIX_MONTHS] → 6건, type 2 2회 | 예 — "5건" 기대를 대면 거짓 → 유효 | Playwright 요청 기록 |
| B-2-보조 | 통과 | 응답 값이 화면 숫자와 그대로 일치한다. 프론트가 지어낸 값이 남아 있지 않다 | 보조 조합: 005930 / 적극투자형(MACD) / 1년 — 거래가 있어 0이 아닌 값 비교용. 응답 finalScore 69.0265, totalReturn 1.15, annualReturn 1.22, mdd 0.29, sharpe 2.39, volatility 0.51, positionCount 8 → 화면 누적수익률 115.00% · 연평균수익률 122.00% · 최대낙폭 (MDD) 29.00% · 샤프비율) 2.39 · 변동성) 51.00% · 거래횟수 8회. 어긋난 항목: 없음. 축 점수(안정성·수익성·효율성·성장성 100점 환산)는 응답에 없고 프론트가 원시 지표로 계산해 보여준다: "안정성 비중 15% 16 수익성 비중 30% 100 효율성 비중 25% 100 성장성 비중 30% 39" | 예 — 단위를 틀린 값(×100 없이)을 기대하면 거짓 → 유효 | shot-B2-type4.png |
| B-4 | 사람 확인 | positionCount: 0 응답 → 화면이 이걸 "안정적"처럼 보이게 그리는가 기록 | 실제 데이터(005930·이동평균 교차·1년)가 바로 거래 0회다. 화면: "안정성 비중 25% 100", "거래횟수 0회 이 기간에 전략이 사고판 횟수예요 이 기간에는 매매 신호가 한 번도 나오지 않았어요. 손익이 없어 안정성 점수가 높게 잡히니, 적합도보다 거래횟수 0회를 먼저 봐주세요." / 등급 "적합도 31.7/ 100 잘 ". 안정성 100점이 그대로 그려지지만 경고 문장이 붙는다 | 해당 없음 | shot-B4-zero-trades.png |
| B-5 | 사람 확인 | 적합/보통/부적합 등급 → 경계 점수에서 어느 쪽으로 떨어지는지 기록 | 등급 3종 [잘 안 맞아요 / 무난해요 / 잘 맞아요]. 5점 간격: 0:잘 안 맞아요, 5:잘 안 맞아요, 10:잘 안 맞아요, 15:잘 안 맞아요, 20:잘 안 맞아요, 25:잘 안 맞아요, 30:잘 안 맞아요, 35:잘 안 맞아요, 40:잘 안 맞아요, 45:잘 안 맞아요, 50:무난해요, 55:무난해요, 60:무난해요, 65:무난해요, 70:잘 맞아요, 75:잘 맞아요, 80:잘 맞아요, 85:잘 맞아요, 90:잘 맞아요, 95:잘 맞아요, 100:잘 맞아요. 경계 근처: 45:잘 안 맞아요, 46:잘 안 맞아요, 47:잘 안 맞아요, 48:잘 안 맞아요, 49:잘 안 맞아요, 50:무난해요, 49.94:잘 안 맞아요, 49.95:잘 안 맞아요, 49.96:잘 안 맞아요, 49.99:잘 안 맞아요, 65:무난해요, 66:무난해요, 67:무난해요, 68:무난해요, 69:무난해요, 70:잘 맞아요, 69.94:무난해요, 69.95:무난해요, 69.96:무난해요, 69.99:무난해요. 범위 밖: -5:"-5.0/100 잘 안 맞아요"(보충 확인), 100.5:잘 맞아요, 150:잘 맞아요. 화면 표시는 소수 첫째 자리 반올림이라, 표시 숫자는 같아도 등급이 다른 값이 있는지 위 소수 경계에서 확인 → 경계는 50·70점(이상이면 위 등급). 49.95~49.99는 화면에 "50.0 / 100"으로 반올림돼 보이는데 등급은 "잘 안 맞아요", 50.00은 같은 "50.0"인데 "무난해요". 69.95~69.99도 "70.0"으로 보이면서 "무난해요". 100을 넘는 값은 "100.5 / 100"처럼 그대로 보인다 | 해당 없음(기록 항목) | Playwright |
| B-6 | 문서와 다름 | 커밋 0cf23ba 이후 화면 어디에도 "안정형이니까 ○○ 전략" 식 문구가 없다 | [비로그인] 시작 전 선택된 전략 "투자전략을 선택해주세요." · 문장: "주식 백테스트 이용하기 로그인하고 투자성향테스트를 하면 나에게 맞는 전략을 자동으로 골라드려요." / "삼성전자는 어떤 성향에 맞나요? 1년 기준으로 다섯 전략을 비교 최고 적극투자형 69." / "위험중립형은 이런 스타일이에요. 안정성과 수익성 사이에서 균형을 추구하며 시장 평균 수준의 위험을 감수하는 스타일 그래서 샤프비율과 연평균수익률를 특히 눈여겨봐요." ‖ [로그인(위험중립형)] 시작 전 선택된 전략 "이동평균 교차 전략" · 문장: "백테스트 시작하기 백테스트 결과 백테스트 다시하기 나는 위험중립형, 여기에 맞는 이동평균 교차 전략으로 분석했어요." / "삼성전자는 어떤 성향에 맞나요? 1년 기준으로 다섯 전략을 비교 최고 적극투자형 69." / "위험중립형은 이런 스타일이에요. 안정성과 수익성 사이에서 균형을 추구하며 시장 평균 수준의 위험을 감수하는 스타일 그래서 샤프비율과 연평균수익률를 특히 눈여겨봐요." — 0cf23ba가 뺀 "성향에 맞는 전략을 미리 골라 두기"가 로그인 상태에서 다시 동작하고(시작 전 이동평균 교차 전략이 선택돼 있음) 결과 문장이 성향→전략 인과를 말한다. 비로그인 배너도 "나에게 맞는 전략을 자동으로 골라드려요". 참고: CLAUDE.md는 현재 "성향 → 전략은 1:1(백엔드 switch)"을 사실로 적고 있어, 0cf23ba 이후 방침이 바뀐 것인지는 사람이 판단할 부분 ‖ 커밋 0cf23ba는 이 브랜치에 없다(origin/feat/backtest-strategy-cards에만 있음). 이 브랜치의 로그인 시 전략 자동 선택은 dae25f3(2026-09-18 "백테스트를 실제 프리셋에 연결")에서 들어왔다(BacktestPage.tsx:159-166). 그래서 "0cf23ba 이후"라는 전제가 이 브랜치에 성립하지 않는다 | 해당 없음 | DOM 텍스트 |
| B-7 | 사람 확인 | 900px·600px 너비 → 가로 스크롤 없음, 겹침 없음 (스크린샷 + 사람 확인) | 900px: 문서 폭 900 / 화면 900 · 600px: 문서 폭 600 / 화면 600 | 해당 없음 | shot-B7-900.png, shot-B7-600.png |
| B-상태-options500 | 통과 | options 500 → 화면이 살아 있고 안내가 있다 | 화면 살아 있음. 전략을 고르면 기간 목록이 서버 대신 프론트의 최소 기간 표로 채워진다(이동평균 교차 → 6개월·1년). 사용자에게 알리지는 않음 | 예 — 오류화면 감지 판정식은 B-상태-null·M-상태-null에서 거짓을 냈다 → 유효 (이 항목은 "화면이 살아 있다" 부분만 변이로 확인) | shot-B-상태-options500.png |
| B-상태-options-null | 통과 | options result: null → 화면이 살아 있다 | options 500과 같다: 프론트 최소 기간 표로 채워지고 화면 살아 있음 | 예 — 오류화면 감지 판정식은 B-상태-null·M-상태-null에서 거짓을 냈다 → 유효 (이 항목은 "화면이 살아 있다" 부분만 변이로 확인) | shot-B-상태-options-null.png |
| B-상태-느림 | 통과 | preset 2.5초 지연 → 로딩 표시, 버튼 연타 방지 | 0.6초 시점 시작 버튼이 "불러오는 중…"으로 바뀌고 잠김. 강제로 한 번 더 눌러도 preset 6건 그대로(연타 방지). 2.5초 뒤 결과 표시 | 예 — 결과가 뜬 뒤엔 버튼이 풀려 "잠김" 판정 거짓 → 유효 | shot-B-상태-느림.png |
| B-상태-500 | 통과 | preset 500 → 실패 안내 | 실패 안내 + 다시 시도 버튼. 안내 문구는 서버 message를 그대로 쓴다: "서버 에러, 관리자에게 문의 바랍니다." / 빈 message면 "요청 실패 (500)" / 서버가 예외 문자열을 주면 "java.lang.NullPointerException: …"까지 그대로 노출(추가 발견 참고) | 예 — 정상 응답엔 "다시 시도"가 없어 판정 거짓 → 유효 | shot-B-상태-500.png |
| B-상태-끊김 | 통과 | preset 끊김 → 실패 안내 | "결과를 불러오지 못했어요. 잠시 후 다시 시도해 주세요." + 다시 시도 | 예 — 정상 응답엔 없음 → 유효 | shot-B-상태-끊김.png |
| B-상태-404_5 | 통과 | BACKTEST404_5 → "아직 계산되지 않은 조합" 안내 | "아직 계산되지 않은 조합이에요. 매일 새벽 4시에 계산돼요. 이 종목은 일봉이 모자라 건너뛰었을 수 있어요." + 다시 시도 | 예 — 500일 때와 문구가 달라 코드별 분기 확인 → 유효 | shot-B-상태-404_5.png |
| B-상태-null | 실패 | preset result: null → 오류 화면 아님, 안내 | preset 응답 result: null → 화면 전체가 오류 화면("화면을 표시하지 못했어요")으로 넘어간다. 입력 폼·가이드까지 사라짐 | 해당 없음(실패) | shot-B-상태-null.png |
| B-상태-지표null | 실패 | result.result.profit: null → 오류 화면 아님 | preset 응답의 result.result.profit 하나만 null이어도 화면 전체가 오류 화면으로 넘어간다 | 해당 없음(실패) | shot-B-상태-지표null.png |
| B-상태-비교일부실패 | 통과 | 비교용 5건 중 1건(type 5)만 500 → 고른 전략 결과는 뜨고 비교만 비거나 표시 | 고른 전략 결과는 그대로 뜨고, 비교 목록에서 실패한 공격투자형(type 5)만 조용히 빠진다(4개만 표시, 빠졌다는 표시 없음). 반대로 고른 전략 요청(두 번째 type 3)만 실패하면 비교용으로 이미 받은 같은 데이터가 있어도 결과 전체가 실패 안내로 바뀐다(B-3의 중복 요청이 실패 지점을 하나 더 만든다) | 예 — 정상 응답엔 비교 5개 → 4개 판정 거짓 → 유효 | shot-B-상태-비교일부실패.png |
| B-상태-종목목록실패 | 통과 | GET /api/stocks 500 → 종목 검색이 예비 목록으로라도 된다 | GET /api/stocks 500이어도 종목 검색이 예비 목록(stockList.ts)으로 돼 삼성전자를 고를 수 있고 백테스트가 끝까지 돈다 | 예 — 오류화면 감지 판정식은 B-상태-null·M-상태-null에서 거짓을 냈다 → 유효 (이 항목은 "화면이 살아 있다" 부분만 변이로 확인) | shot-B-상태-종목목록실패.png |
| A-1 | 문서와 다름 | 질문 전송 → 가로챈 GET /api/open-ai/ask에 질문·종목명이 올바르게 실린다 | 메서드는 GET이 아니라 POST다. 실린 본문: POST {"question":"삼성전자 앞으로 전망 어때?","stockName":"삼성전자"} auth=Bearer… / POST {"question":"요즘 반도체 업황은 어때?","stockName":null} auth=Bearer… / POST {"question":"sk하이닉스랑 LG에너지솔루션 중에 뭐가 나아?","stockName":"LG에너지솔루션"} auth=Bearer…. 종목 입력칸이 없고 질문 글에서 종목명을 뽑아 싣는다. 첫 질문(삼성전자) 기준 질문·종목명 일치: 예 참고: "sk하이닉스랑 LG에너지솔루션"은 소문자 sk를 못 알아봐 LG에너지솔루션만 실렸다 | "stockName=SK하이닉스" 기대를 대면 거짓 → 유효 | Playwright 요청 본문 |
| A-2 | 실패 | 답변 자리: 빈 말풍선도 지어낸 답도 아니고 "서버가 아직 답을 돌려주지 않는다" 안내 | 지시서의 "서버가 아직 답을 돌려주지 않는다" 전제는 지났다(답은 실제 서버가 준다). 남은 요구인 "빈 말풍선 없음"을 봤다: answer가 ""·null·공백이면 회색 빈 말풍선이 남는다(다시 시도도 안내도 없음). result 자체가 null이면 "답변을 가져오지 못했어요. 다시 시도" 말풍선. 정상 답은 그대로 표시 | 정상 답에선 빈 말풍선이 없어 판정이 뒤집힘 → 유효 | shot-A2-answer"".png, shot-A2-answernull.png, shot-A2-answer공백.png |
| A-3 | 보류 | 차단 경로에서 실패 → "다시 시도" 버튼이 나오지 않는다 | "차단 경로"(API_DISABLED)는 현재 꺼져 있고 테스트를 위해 켜는 것은 금지라 그대로는 확인 불가. 대신 다시 해도 소용없는 4xx 실패를 넣어 봄: [404 MEMBER404_2(성향 없음)] 다시 시도 버튼 0개 · "삼성전자 전망? 투자성향을 먼저 정해야 답할 수 있어요.투자성향 검사하기 보내기" · 링크 투자성향 검사하기→/personality-test?from=ai ‖ [400] 다시 시도 버튼 1개 · "삼성전자 전망? 답변을 가져오지 못했어요. 다시 시도 보내기" ‖ [403] 다시 시도 버튼 1개 · "삼성전자 전망? 답변을 가져오지 못했어요. 다시 시도 보내기" | 해당 없음 | DOM |
| A-4 | 통과 | 일시적 실패(500) → "다시 시도"가 나온다 | 500 → 다시 시도 버튼 1개, 실패 시 "삼성전자 전망? 답변을 가져오지 못했어요. 다시 시도 보내기" → 누르면 같은 본문으로 재요청(2회) 후 "삼성전자 전망? 재시도 성공 답변 보내기". 내 질문 말풍선 1개(중복 여부) | 예 — 4xx(A-3)에서는 버튼이 안 나오므로 "항상 버튼" 판정은 거짓 → 유효 | DOM |
| A-5 | 사람 확인 | 왼쪽 대화 목록이 mock이라는 표시가 있는가 (기록 후 사람 확인) | 목록 "이전 대화내용 + 새 대화 한화디펜스 전쟁 영향 HYBE 콘서트 성공 여부 LG에너지솔루션 분석 에스오일 석유화학 관련 SK하이닉스 사업분석 GS건설 수주사업" — "예시·샘플" 같은 표시 없음(/예시\|샘플\|mock\|데모/ 검색 0건: 맞음). 첫 대화를 누르면 "한화디펜스 전쟁 영향에 대해 알려줘. (예시 응답) 한화디펜스 전쟁 영향 관련해서 확인된 내용을 정리해 드릴게요. 아직 백엔드 연동 전이라 실제 분석 결과가 아닙니다. 연동이 끝나면 이 자리에 뉴스 검색(RAG)을 거친 답변이 표시됩니다. 그럼 지금 사는 건 어때? (예시 응답) 투자 판단은 직접 하셔야 해요. 다만 확인해두면 좋은 지표는 알려드릴 수 있습". 이때 나간 요청 [GET /api/members/me/personality] — 목록·내용은 서버 요청 없이 그려진다 | 해당 없음 | shot-A5.png |
| A-6 | 사람 확인 | 긴 대화 → 사이드바 높이가 어색해지는 구간 스크린샷 | 1회: 사이드바 370px / 대화 620px / 문서 900px · 2회: 사이드바 370px / 대화 620px / 문서 900px · 3회: 사이드바 370px / 대화 620px / 문서 900px · 4회: 사이드바 370px / 대화 620px / 문서 900px · 5회: 사이드바 370px / 대화 620px / 문서 900px · 6회: 사이드바 370px / 대화 620px / 문서 900px | 해당 없음 | shot-A6-2.png, shot-A6-6.png |
| A-상태-느림 | 통과 | 답변 2.5초 지연 → 기다리는 표시, 보내기 연타 방지 | 0.3초 시점 "답변을 생성하고 있어요" 말풍선, 보내기 버튼 잠김·입력칸 편집 불가(Playwright fill이 첫 답이 올 때까지 2.3초 기다림). 첫 실행 기록의 "잠김=false"는 입력칸이 풀린 뒤 잰 값이라 무효. 대화방을 옮긴 사이 도착한 답은 다른 방에 섞이지 않음 | 예 — 답이 온 뒤엔 잠김이 풀려 판정 거짓 → 유효 | shot-A-상태-느림.png |
| A-상태-끊김 | 통과 | 끊김 → 실패 안내 + 다시 시도 | /ai · 오류화면 안 뜸 · 질문 요청 1건 · 토큰 남음 · "AI 주가분석 이용하기 이전 대화내용 + 새 대화 한화디펜스 전쟁 영향 HYBE 콘서트 성공 여부 LG에너지솔루션 분석 에스오일 석유화학 관련 SK하이닉스 사업분석 GS건설 수주사업 삼성전자 전망? 답변을 가져오지 못했어요. 다시 시도 보내기" | 예 — 정상 답엔 다시 시도가 없음 → 유효 | shot-A-상태-끊김.png |
| A-상태-성향500 | 통과 | 진입 시 GET /members/me/personality 500 → 화면이 살아 있다 | /ai · 오류화면 안 뜸 · 질문 요청 0건 · 토큰 남음 · "AI 주가분석 이용하기 이전 대화내용 + 새 대화 한화디펜스 전쟁 영향 HYBE 콘서트 성공 여부 LG에너지솔루션 분석 에스오일 석유화학 관련 SK하이닉스 사업분석 GS건설 수주사업 JUBY AI 도우미 투자성향을 정하면 나에게 맞춘 답을 받을 수 있어요. 투자성향 검사하기 보내기" — 화면은 살지만, 성향이 있는데 조회만 실패한 경우도 "성향 없음"과 같은 문구로 보인다 | 예 — 정상 성향이면 "현재 당신의 투자성향은 ‘위험중립형’"이라 문구가 달라 판정 거짓 → 유효 | shot-A-상태-성향500.png |
| A-상태-성향null | 통과 | 성향 result: null → 화면이 살아 있다 | /ai · 오류화면 안 뜸 · 질문 요청 0건 · 토큰 남음 · "AI 주가분석 이용하기 이전 대화내용 + 새 대화 한화디펜스 전쟁 영향 HYBE 콘서트 성공 여부 LG에너지솔루션 분석 에스오일 석유화학 관련 SK하이닉스 사업분석 GS건설 수주사업 JUBY AI 도우미 투자성향을 정하면 나에게 맞춘 답을 받을 수 있어요. 투자성향 검사하기 보내기" | 예 — 정상 성향이면 "현재 당신의 투자성향은 ‘위험중립형’"이 떠 같은 판정식이 거짓 | shot-A-상태-성향null.png |
| A-상태-성향404 | 통과 | 성향 없음(404 MEMBER404_2) → 성향테스트로 안내 | /ai · 오류화면 안 뜸 · 질문 요청 0건 · 토큰 남음 · "AI 주가분석 이용하기 이전 대화내용 + 새 대화 한화디펜스 전쟁 영향 HYBE 콘서트 성공 여부 LG에너지솔루션 분석 에스오일 석유화학 관련 SK하이닉스 사업분석 GS건설 수주사업 JUBY AI 도우미 투자성향을 정하면 나에게 맞춘 답을 받을 수 있어요. 투자성향 검사하기 보내기" | 예 — 정상 성향이면 "현재 당신의 투자성향은 ‘위험중립형’"이 떠 같은 판정식이 거짓 | shot-A-상태-성향404.png |
| A-상태-비로그인 | 통과 | 토큰 없이 /ai → 로그인으로 보내거나 안내 | /ai · 오류화면 안 뜸 · 질문 요청 0건 · 토큰 없음 · "AI 주가분석 이용하기 이전 대화내용 + 새 대화 로그인하면 이전 대화를 저장할 수 있어요. JUBY AI 도우미 보내기" · 글을 쓰고 보내기를 누르면 요청 없이 "로그인하면 질문할 수 있어요."로 바뀐다(입력한 글은 남음) | 예 — 로그인 상태에선 요청이 나가 판정 거짓 → 유효 | shot-A-상태-비로그인.png |
| A-상태-401 | 통과 | 질문 POST 401 → 토큰 삭제 후 로그인으로 | /login · 오류화면 안 뜸 · 질문 요청 1건 · 토큰 없음 · "로그인 초보자를 위한 주식 비서, JUBY의 세계로! Create Your Own JUBY!" | 예 — 500(A-4)에선 머무르므로 판정 거짓 → 유효 | shot-A-상태-401.png |
| A-상태-빈질문 | 통과 | 공백만 입력 → 보내지 않는다 | /ai · 오류화면 안 뜸 · 질문 요청 0건 · 토큰 남음 · "AI 주가분석 이용하기 이전 대화내용 + 새 대화 한화디펜스 전쟁 영향 HYBE 콘서트 성공 여부 LG에너지솔루션 분석 에스오일 석유화학 관련 SK하이닉스 사업분석 GS건설 수주사업 JUBY AI 도우미 현재 당신의 투자성향은 ‘위험중립형’ 입니다. 투자성향 변경하기 보내기" · 공백 입력 뒤 보내기 잠김=true | 예 — 글자를 넣으면 풀림 → 유효 | shot-A-상태-빈질문.png |
| M-1 | 통과 | 토큰 없이 진입 → 로그인 화면으로 보낸다 | /mypage→/login(요청 0건), /mypage/profile→/login(요청 0건), /mypage/personality→/login(요청 0건), /mypage/likes→/login(요청 0건) | 예 — 토큰이 있으면 /mypage/profile에 머물러 판정 거짓 → 유효 | URL |
| M-2 | 통과 | GET /members/me 401 → 토큰을 지우고 로그인으로 보낸다 | /login, 토큰 없음, 머리글 "주식 백테스트 AI 주가분석 JUBY 사용설명서 로그인" | 예 — 500(M-3)에선 머무르고 토큰이 남아 판정 거짓 → 유효 | URL·localStorage |
| M-3 | 통과 | GET /members/me 500 (인증 실패를 500으로 주는 경우) → "로그인이 풀렸을 수 있다" 안내. 무한 재시도 없음 | 5초 동안 요청 1회(무한 재시도 없음). 안내 "회원 정보를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요. 문제가 계속되면 다시 로그인해 보세요. 다시 시도" — 로그인이 풀렸을 가능성 언급: 있음 · 다시 누르면 요청 2회. 참고: CLAUDE.md는 이제 백엔드가 인증 실패에 제대로 401을 준다고 적고 있어, 지시서의 "500으로 준다" 전제는 지난 것일 수 있다 | 예 — 정상 응답엔 "다시 로그인해 보세요"가 없어 판정 거짓 → 유효 | shot-M3.png |
| M-4 | 사람 확인 | 이름 5자 이상 회원이 생년월일만 수정 → 어떤 에러가 어떻게 보이는지 기록 | 서버가 준 이름 "남궁민수호"(5자)가 최대 4자 입력칸에 "남궁민수호"로 들어간다. 생년월일만 바꾸고 저장(버튼 잠김=false) → PATCH 0건  → 모달 "내 정보 수정 이름 2~4자 생년월일 비워 두면 정보 없음으로 저장돼요 이름은 2~4자여야 합니다. 저장 취소" — 요청 전에 프론트가 이름 길이로 막는다. 생년월일만 고치려는 5자 이름 회원은 이름을 4자 이하로 바꾸지 않으면 저장할 수 없다(서버 2~4자 규칙과 같은 결과를 프론트가 먼저 낸다) | 해당 없음(기록 항목) | shot-M4.png |
| M-5 | 통과 | 수정 폼 잘못된 생년월일(미래 날짜, 2026-02-30) → 요청 전에 막힌다 | 미래 2030-01-01(값 직접 입력) → 입력칸 max 넘음, PATCH 0건, 브라우저 경고 "값은 2026. 09. 23. 이전이어야 합니다." / 없는 날 2026-02-30·2001-02-29(키보드) → 입력칸 값이 ""(잘못된 입력), PATCH 0건, 브라우저 경고 "유효한 값을 입력하세요…" / 정상 1990-05-05 → PATCH 1건. 막는 주체는 앱 문구가 아니라 브라우저 기본 검증 말풍선이다. 참고: 첫 실행의 "미래" 칸은 키보드 조작이 연도 칸에 잘못 들어가 0203-01-01이 된 것이라 판정에서 뺐다 | 예 — 정상 날짜는 요청이 나가 "막힘" 판정 거짓 → 유효 | Playwright 요청 기록 |
| M-6 | 통과 | 탈퇴 → 확인 단계가 있다. 성공 후 토큰 삭제 + 홈 이동 | 확인 모달 "정말 탈퇴하시겠습니까? 계정과 투자성향 정보가 모두 삭제되며 복구할 수 없습니다. 탈퇴하기 취소"(누르기 전 DELETE 0건) → 탈퇴 후 /, 토큰 없음, 머리글 "주식 백테스트 AI 주가분석 JUBY 사용설명서 로그인". 참고: 탈퇴 DELETE가 500이면 /mypage/profile에 머물고 토큰 유지, 모달 "정말 탈퇴하시겠습니까? 계정과 투자성향 정보가 모두 삭제되며 복구할 수 없습니다. 탈퇴하지 못했습니다. 잠시 후 다시 시도해 주세요. 탈퇴하기 취소" | 예 — 실패(500) 경우엔 토큰이 남아 판정 거짓 → 유효 | URL·localStorage |
| M-7 | 통과 | PATCH /members/me/personality 요청이 나가지 않는다 (personalityId를 추측해서 보내지 않음) | 마이페이지 → 검사 다시하기 → 10문항 → /personality-test/result → 완료 → /mypage/personality. PATCH /members/me/personality 0건. 나간 요청: GET /api/members/me/personality, GET /api/personality-tests, POST /api/personality-tests, GET /api/members/me/personality | 예 — 같은 흐름에서 POST /personality-tests는 1건 잡혀 요청 감지가 동작함을 확인 → 유효 | Playwright 요청 기록 |
| M-상태-느림 | 통과 | 회원정보 2.5초 지연 → 로딩 표시 | 0.9초 시점 내용 구역에 스켈레톤(div.skeleton) 1개, 글자 없음 → 2.5초 뒤 회원 정보 표시 | 예 — 다 뜬 뒤엔 스켈레톤 0개 → 유효 | shot-M-상태-느림.png |
| M-상태-null | 실패 | 회원정보 result: null → 화면이 살아 있다 | GET /members/me가 result: null → TypeError: Cannot read properties of null (reading 'birth') → 화면 전체가 오류 화면(마이페이지 사이드바·로그아웃까지 사라짐) | 해당 없음(실패) | shot-M-상태-null.png |
| M-상태-끊김 | 통과 | 회원정보 끊김 → 실패 안내 | 오류화면 안 뜸 · 내용 "회원 정보를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요. 문제가 계속되면 다시 로그인해 보세요. 다시 시도" | 예 — 정상 응답엔 안내 없음 → 유효 | shot-M-상태-끊김.png |
| M-상태-빈값 | 통과 | 이름·이메일·생일이 빈 값/null → 깨지지 않는다 | 오류화면 안 뜸 · 내용 "생년월일 정보 없음 정보 수정하기 회원 탈퇴하기" — 화면은 살지만 이름·이메일 줄은 자리째 사라지고 생년월일만 "정보 없음"으로 대체된다 | 예 — 오류화면 감지 판정식은 B-상태-null·M-상태-null에서 거짓을 냈다 → 유효 (이 항목은 "화면이 살아 있다" 부분만 변이로 확인) | shot-M-상태-빈값.png |
| M-상태-수정500 | 통과 | 정보 수정 PATCH 500 → 모달이 남고 실패 안내 | 오류화면 안 뜸 · 내용 "홍길동 2000년 01월 01일 tester@example.com 정보 수정하기 회원 탈퇴하기 내 정보 수정 이름 2~4자 생년월일 비워 두면 정보 없음으로 저장돼요 저장하지 못했습니다. 잠시 후 다시 시도해 주세요. 저장 취소" · 저장 뒤 모달 "내 정보 수정 이름 2~4자 생년월일 비워 두면 정보 없음으로 저장돼요 저장하지 못했습니다. 잠시 후 다시 시도해 주세요. 저장 취소" | 예 — 정상 저장이면 모달이 닫혀 판정 거짓 → 유효 | shot-M-상태-수정500.png |
| M-상태-성향500 | 통과 | 투자유형 GET 500 → 실패 안내 | 오류화면 안 뜸 · 내용 "투자성향을 불러오지 못했습니다. 잠시 후 다시 시도해 주세요. 문제가 계속되면 다시 로그인해 보세요. 다시 시도" | 예 — 정상 응답엔 안내 없음 → 유효 | shot-M-상태-성향500.png |
| M-상태-성향null | 통과 | 투자유형 result: null → 화면이 살아 있다 | 오류화면 안 뜸 · 내용 "아직 투자성향 검사를 하지 않았습니다 일곱 문항이면 끝나요. 검사하고 나면 여기에 결과가 남습니다. 검사하러 가기" — 문구의 "일곱 문항이면 끝나요"는 실제 문항 수(10개, P-1)와 다르다 | 예 — 정상 성향 응답(기준 실행)에는 "아직 투자성향 검사를 하지 않았습니다"가 없어 판정 거짓 | shot-M-상태-성향null.png |
| M-상태-성향404 | 실패 | 성향 없음(404 MEMBER404_2) → 검사로 안내 | 백엔드 MemberService.getPersonalityInfo는 성향이 없는 회원에게 404 MEMBER404_2를 준다(origin/dev 확인) = 갓 가입한 모든 회원. 그런데 /mypage/personality(마이페이지 기본 탭)는 이걸 "투자성향을 불러오지 못했습니다. 잠시 후 다시 시도해 주세요. 문제가 계속되면 다시 로그인해 보세요. 다시 시도"로 그린다. 같은 404를 /backtest는 "투자성향테스트를 아직 안 하셨어요", /ai는 "투자성향을 정하면…"으로 제대로 받는다. "아직 검사하지 않았습니다" 화면은 result: null일 때만 나온다 | 해당 없음(실패) | shot-M-상태-성향404.png |
| M-상태-관심빈배열 | 통과 | 관심종목 0개 → "없다"류 안내 | 오류화면 안 뜸 · 내용 "아직 관심종목이 없어요 홈 시세표에서 하트를 누르면 여기에 모여요. 종목 보러 가기" | 예 — 정상 목록엔 안내 없음 → 유효 | shot-M-상태-관심빈배열.png |
| M-상태-관심500 | 통과 | 관심종목 500 → 실패 안내 | 오류화면 안 뜸 · 내용 "관심종목을 불러오지 못했습니다. 다시 시도" | 예 — 정상 목록엔 안내 없음 → 유효 | shot-M-상태-관심500.png |
| M-상태-관심null | 통과 | 관심종목 result: null → 화면이 살아 있다 | 오류화면 안 뜸 · 내용 "관심종목을 불러오지 못했습니다. 다시 시도" | 예 — 정상 목록엔 안내 없음 → 유효 | shot-M-상태-관심null.png |
| M-상태-해제실패 | 실패 | 관심종목 해제 DELETE 500 → 목록에 되돌리고 알린다 | 해제 누른 직후 목록에서 빠졌다가(1→0줄) 실패 뒤 되돌아온다(0→1줄). 되돌린 이유를 알리는 문구·role=alert/status는 없다 — 화면 글자가 누르기 전과 완전히 같다. 홈 시세표 하트도 같은 동작(추가 → 해제 → 추가로 조용히 복귀) | 되돌림 자체는 확인(누른 직후 0줄) → 판정 유효 | shot-M-상태-해제실패.png |
| L-1 | 문서와 다름 | 소셜 버튼 클릭 → "막아 뒀다" 알림. 외부로 이동하지 않는다 | LoginPage에 "막아 둔" 잠금이 없다. 누르면 곧바로 백엔드 OAuth 시작 주소로 이동한다(테스트가 가로채서 실제로는 안 감): 네이버: 대화상자 [] · 이동 시도 (가로챔) http://3.35.191.42:8080/oauth2/authorization/naver , http://3.35.191.42:8080/oauth2/authorization/naver ‖ 카카오: 대화상자 [] · 이동 시도 (가로챔) http://3.35.191.42:8080/oauth2/authorization/kakao , http://3.35.191.42:8080/oauth2/authorization/kakao ‖ 구글: 대화상자 [] · 이동 시도 (가로챔) http://3.35.191.42:8080/oauth2/authorization/google , http://3.35.191.42:8080/oauth2/authorization/google | 해당 없음 | Playwright 이동 기록 |
| L-2 | 통과 | /oauth/callback?accessToken=a&refreshToken=b 직접 진입 → 토큰 저장 → 주소창에서 토큰이 지워진다 → 홈 이동 → 헤더가 로그인 상태 | 저장 a/b · 주소 http://localhost:4173/ · 머리글 "주식 백테스트 AI 주가분석 JUBY 사용설명서 마이페이지" | 예 — 토큰 하나가 빠진 L-3에서는 판정이 거짓 → 유효 | URL·localStorage |
| L-4 | 통과 | 콜백 주소가 방문기록에 남는지 — history.replaceState 등으로 토큰이 지워지는지 기록 | /login → 콜백 → 홈 뒤 history.length 3. 뒤로가기 한 번 → http://localhost:4173/login (토큰 붙은 주소로 돌아가는가: 아니오) | 예(기대값 변이) — 판정식에 "토큰 붙은 주소로 돌아간다"를 기대로 대면 거짓. 반대 상황(replace 없는 이동)은 코드 수정이 필요해 돌리지 않음 | URL |
| L-3 | 실패 | 콜백에 토큰 하나 누락 / 빈 값 → 저장하지 않고 에러 안내 | [accessToken=a] 저장 안 됨 · /login "로그인 초보자를 위한 주식 비서, JUBY의 세계로! Create Your Own JUBY! 로그인에 실패했습니다. 다시 시도" ‖ [refreshToken=b] 저장 안 됨 · /login "로그인 초보자를 위한 주식 비서, JUBY의 세계로! Create Your Own JUBY! 로그인에 실패했습니다. 다시 시도" ‖ [accessToken=&refreshToken=b] 저장 됨(/b) · / "검색 백테스트 기법으로 투자한 (??? 멘트 수정예정 - 광엽) 테마별 대표 종목 기술주 대장 SK하이닉스 +0.1% 한 달 " · 머리글 로그인 상태 ‖ [accessToken=a&refreshToken=] 저장 됨(a/) · / "검색 백테스트 기법으로 투자한 (??? 멘트 수정예정 - 광엽) 테마별 대표 종목 기술주 대장 SK하이닉스 +0.1% 한 달 " · 머리글 로그인 상태 ‖ [accessToken=%20&refreshToken=%20] 저장 됨( / ) · / "검색 백테스트 기법으로 투자한 (??? 멘트 수정예정 - 광엽) 테마별 대표 종목 기술주 대장 SK하이닉스 +0.1% 한 달 " · 머리글 로그인 상태 ‖ [accessToken=null&refreshToken=undefined] 저장 됨(null/undefined) · / "검색 백테스트 기법으로 투자한 (??? 멘트 수정예정 - 광엽) 테마별 대표 종목 기술주 대장 SK하이닉스 +0.1% 한 달 " · 머리글 로그인 상태 ‖ [error=access_denied] 저장 안 됨 · /login "로그인 초보자를 위한 주식 비서, JUBY의 세계로! Create Your Own JUBY! 로그인에 실패했습니다. 다시 시도" ‖ [(쿼리 없음)] 저장 안 됨 · /login "로그인 초보자를 위한 주식 비서, JUBY의 세계로! Create Your Own JUBY! 로그인에 실패했습니다. 다시 시도" | L-2(정상 두 값)에서는 저장돼 판정이 거짓 → 유효 | localStorage |
| L-5 | 보류 | 실제 소셜 로그인 | 백엔드 OAuth2SuccessHandler가 프론트로 리다이렉트하지 않는다(CLAUDE.md 2026-09-18). 실제 계정 로그인이 필요하고 백엔드 수정 전이라 확인 불가 | 해당 없음 | CLAUDE.md |
| G-1 | 사람 확인 | MOCK_SECTIONS가 그려진다. 예시 내용이라는 게 드러나는가 | "JUBY 사용설명서 1 JUBY 사용법 준비중입니다." · /api 요청 0건. 내용이 "준비중입니다" 한 줄이라 예시라기보다 비어 있음이 드러난다(사용설명서 본문은 사용자가 직접 작성 예정) | 해당 없음 | shot-G1.png |
| G-2 | 보류 | 서버 플래그 켠 상태(가로채기 404) → 화면이 깨지지 않고 안내가 나온다 | USE_BACKEND_GUIDE가 코드 상수(false)라 켜려면 소스를 고쳐야 한다. 지시서가 코드 수정을 금지해 확인 불가. 꺼진 상태에서는 /api 요청 자체가 없다 | 해당 없음 | src/api/guide.ts:7 |
| C-1 | 통과 | 없는 주소 /asdf → 404 화면 | /asdf → /asdf "없는 주소예요 주소를 잘못 입력했거나 지금은 사라진 페이지예요. 홈에서 " 탭 "페이지를 찾을 수 없음 - JUBY" ‖ /stocks → /stocks "없는 주소예요 주소를 잘못 입력했거나 지금은 사라진 페이지예요. 홈에서 " 탭 "페이지를 찾을 수 없음 - JUBY" ‖ /mypage/unknown → /mypage/unknown "없는 주소예요 주소를 잘못 입력했거나 지금은 사라진 페이지예요. 홈에서 " 탭 "페이지를 찾을 수 없음 - JUBY" ‖ /personality-test/xyz → /personality-test/xyz "없는 주소예요 주소를 잘못 입력했거나 지금은 사라진 페이지예요. 홈에서 " 탭 "페이지를 찾을 수 없음 - JUBY" ‖ /%ED%95%9C%EA%B8%80 → /%ED%95%9C%EA%B8%80 "없는 주소예요 주소를 잘못 입력했거나 지금은 사라진 페이지예요. 홈에서 " 탭 "페이지를 찾을 수 없음 - JUBY" | 예 — 홈에 같은 판정을 대면 거짓 → 유효 | DOM |
| C-2 | 통과 | 모든 화면 콘솔: 가로챈 정상 응답 기준으로 에러·경고 0건 | 운영·개발 각 11개 화면(+백테스트 실행, 수정 모달, 검색 자동완성) 콘솔 에러·경고 0건 | 예 — 같은 수집기가 H-상태-500 등에서 "Failed to load resource"와 M-상태-null의 TypeError를 잡아냈다 → 수집기 유효 | Playwright console |
| C-3 | 실패 | 키보드만으로 주요 동작: 탭 이동으로 버튼·입력에 닿는다. 포커스 표시가 보인다 | 탭 순서: / 24개, /stocks/005930 27개, /personality-test 10개, /backtest 13개, /ai 14개, /mypage/profile 11개, /login 8개 요소에 닿음. 커서가 손가락인데 탭으로 못 닿는 요소 0개. 포커스 표시: 요소 자체 outline/box-shadow로 1차 판정 후, 걸린 4개는 포커스 전후 스크린샷을 비교 — 홈 검색칸·백테스트 검색칸·전략 선택은 감싸는 상자 테두리가 바뀌어 표시 있음(1차 판정 오탐). /ai 질문 입력칸(textarea)만 포커스 전후 화면이 완전히 같다(다른 바이트 0) → 표시 없음 | 같은 비교가 다른 세 칸에서는 변화를 잡아냈다 → 비교 유효 | dbg-c3.mjs, shot-C3-*.png |
| C-4 | 사람 확인 | 탭 아이콘 16px → 알아볼 수 있다 | 아이콘 선언 icon image/svg+xml  /favicon.svg. 16px(흰 바탕·어두운 탭 바탕)과 32px로 그려 스크린샷에 담음 | 해당 없음 | shot-C4-favicon.png |
| W-API-1 | 통과 | 모든 요청이 client.ts 한 곳을 지난다 | fetch( 는 src/api/client.ts:160 한 곳. axios·XMLHttpRequest·WebSocket·sendBeacon 0건. (소셜 로그인은 fetch가 아니라 주소 이동: LoginPage.tsx:54) | 예 — 같은 grep이 client.ts:160을 잡아냄 | grep |
| W-API-2 | 문서와 다름 | API_DISABLED가 허용 경로 외 전부를 막는다(허용 목록 분기 100%) | 허용 목록이 없다. API_DISABLED(client.ts:36)는 켜면 모든 요청을 예외 없이 막는 불리언 하나이고 분기는 client.ts:146 하나뿐. 지금 false라 true 분기는 코드 수정 없이 실행 불가 | 해당 없음 | src/api/client.ts:36,146-150 |
| W-API-3 | 문서와 다름 | /api/backtest-x, /api/personality-tests?x=1, POST 같은 경계 입력이 새지 않는다 | 경로를 가려 보내는 판정식 자체가 없어 경계 입력을 넣을 곳이 없다(W-API-2) | 해당 없음 | src/api/client.ts |
| W-API-4 | 실패 | 401·500·끊김·JSON 파싱 실패가 하나의 에러 모양으로 호출부에 닿는다 | 세 가지 타입으로 갈린다: ok: 성공({"v":1}) · null: 성공(null) · fail200: ApiError(status 200, code X400) "서버가 거절" · s401: ApiError(status 401, code AUTH401) "인증 필요" · s500: ApiError(status 500, code COMMON500) "서버 에러" · s500empty: ApiError(status 500, code null) "요청 실패 (500)" · s502html: Error "요청 실패 (502) /api/qa/s502html" · ok-html: Error "응답을 읽을 수 없습니다 (200) /api/qa/ok-html" · ok-empty: TypeError "Cannot read properties of null (reading 'isSuccess')" · ok-nowrap: ApiError(status 200, code undefined) "" · abort: TypeError "Failed to fetch". 설계상 갈라 둔 것(끊김=Error, 서버 JSON=ApiError, CLAUDE.md)을 빼도 의도 밖 두 가지가 있다 — ①HTTP 200 빈 본문은 unwrap이 null.isSuccess를 읽다 TypeError(client.ts:200-210에서 body=null로 남고 231-233이 검사 없이 읽음) ②래퍼 없는 200은 ApiError인데 message "" · code undefined(타입은 string\|null, client.ts:233) | 해당 없음(실패) | 개발 서버에서 client.ts를 직접 불러 실행(t-unit.mjs) |
| W-API-5 | 통과 | HTTP 200 + isSuccess:false가 성공으로 새지 않는다 | fail200 → ApiError(status 200, code X400, "서버가 거절"). 블랙박스 H-상태-isSuccess거짓·S-상태-isSuccess거짓도 실패 안내로 확인 | 예 — 같은 경로에 isSuccess:true를 주면 성공 값이 나옴(ok 케이스) | client.ts:231-235 |
| W-API-6 | 실패 | /v3/api-docs 스키마와 프론트 타입이 필드 단위로 같다 | 필드 이름·자료형 차이 0건(StockListRes·StockDetailRes·StockNewsRes·MemberInfo·PersonalityInfo·LikeStockListRes·TestQuestionList·TestResultRes·PresetResponse·PresetOptionsResponse·AskResult 대조). 다른 것은 필수 여부: 스웨거 응답 스키마에는 required가 하나도 없는데(요청의 AskRequest.question·LikeStockReq.stockCode만 필수), 프론트 타입은 birth·personalityImg를 빼고 전부 필수·null 불가로 선언(src/types/stock.ts:14-22 등). 그래서 필드가 빠진 응답을 타입 검사가 못 잡고 런타임에서 터진다(추가 발견 1) | 해당 없음(실패) | api-docs.json, src/types/*.ts, src/api/stock.ts:24-51,93-108 |
| W-API-7 | 통과 | 경로 문자열이 한 곳에 모여 있고 화면 코드에 흩어져 있지 않다(개수) | /api/ 경로 문자열 21개가 전부 src/api/*.ts 7개 파일에 있다(ai 3, auth 1, backtest 2, guide 1, member 7, personality 2, stock 5). pages·components 안 0개. 한 파일에 모여 있지는 않고 도메인별 파일로 나뉜다 | 예 — 같은 grep이 21개를 셈 | grep |
| W-AUTH-1 | 통과 | 토큰 접근이 한 곳뿐이다(auth.ts 밖 localStorage 횟수) | localStorage는 src/utils/auth.ts 3곳과 src/utils/cache.ts 2곳뿐(cache.ts는 CLAUDE.md가 정한 캐시 전용). 토큰 키는 auth.ts 밖에 없다 | 예 — grep이 두 파일의 5곳을 잡음 | grep |
| W-AUTH-2 | 통과 | 요청 3개가 동시에 401을 받으면 로그아웃·이동이 3번 일어나는지 | 3건 모두 401로 거절(rejected:401, rejected:401, rejected:401) → 구독자 알림 1회, 주소 이동 1회(replace /login), 최종 /login, 토큰 없음. 첫 401이 토큰을 지운 뒤 나머지는 isLoggedIn()이 false라 되돌아간다(client.ts:129-131) | 예 — 계측이 이동 1건·알림 1건을 실제로 잡았다(여러 번이면 그만큼 잡힘) | t-unit.mjs |
| W-AUTH-3 | 통과 | 로그인 직후 헤더 갱신이 전체 새로고침에 기대는 지점 기록 | 헤더는 useIsLoggedIn 구독으로 바뀌어 새로고침에 기대지 않는다(L-2에서 새로고침 없이 "마이페이지"로 바뀜 확인). 페이지를 통째로 바꾸는 곳은 3곳: navigation.ts:19(맡겨진 navigate가 없을 때의 물러섬), ErrorBoundary.tsx:75(다시 시도), LoginPage.tsx:54(소셜 로그인 외부 이동) | 예 — grep이 3곳을 잡음 | grep |
| W-FN-1 | 실패 | 성향 점수→결과: 10, 90, 경계 ±1, 범위 밖(9, 91), 소수 모두 서버와 같다 | 구간 안 15개는 서버와 같다. 범위 밖 4개는 서버와 다르다 — 서버는 예외를 던지는데 프론트는 결과를 낸다: 9→"안정형", 91→"공격투자형", 0→"안정형", NaN→"공격투자형". scoreToPersonality(src/utils/personality.ts:54-60)에 범위 밖 분기가 없다. 실제 10문항으로는 합이 10~90을 벗어날 수 없고, NaN은 보기가 빈 문항이 있을 때만 생긴다(W-FN-2, P-상태-보기빈배열) | 예 — 범위 밖 기대와 어긋난 4건을 판정이 잡아냄 | t-unit.mjs · src/utils/personality.ts:54-60 |
| W-FN-2 | 통과 | 점수 환산 함수가 10~90에서 입력=출력인지. 맞다면 죽은 코드 후보로 기록 | 실제 10문항, 원점수 10~90(짝수 41개)→41개 모두 입력=출력 · getScoreRange(실제 10문항)→{"min":10,"max":90} · 보기가 빈 문항 하나 섞임→{"min":null,"max":null} · normalizeScore(50, 보기 빈 문항 섞임)→null. 서버 문항 경로에서는 아무 일도 하지 않는다(죽은 코드 후보). 다만 서버가 죽어 예비 7문항으로 물러설 때는 실제로 늘린다(personality.ts:87, 98-100). 보기가 빈 문항이 섞이면 범위가 Infinity라 결과가 NaN(JSON에서 null로 보임). 주석 "확정된 문항은 7개"(personality.ts:14)는 지금 문항 수(10)와 다르다 | 예 — 입력≠출력이면 목록에 남도록 짠 검사가 0건 | t-unit.mjs · src/utils/personality.ts:21-50 |
| W-FN-3 | 통과 | 적합/보통/부적합: 경계값, 음수, NaN, null | 49.94→"잘 안 맞아요 (화면 숫자 49.9)" · 49.95→"잘 안 맞아요 (화면 숫자 50.0)" · 49.99→"잘 안 맞아요 (화면 숫자 50.0)" · 50→"무난해요 (화면 숫자 50.0)" · 69.99→"무난해요 (화면 숫자 70.0)" · 70→"잘 맞아요 (화면 숫자 70.0)" · 100→"잘 맞아요 (화면 숫자 100.0)" · 150→"잘 맞아요 (화면 숫자 150.0)" · -5→"잘 안 맞아요 (화면 숫자 -5.0)" · NaN→"잘 안 맞아요 (화면 숫자 NaN)" · null→"잘 안 맞아요 (화면 숫자 -)" · undefined→"잘 안 맞아요 (화면 숫자 -)". 경계 50·70은 코드(src/utils/backtest.ts:170-182)와 일치. 단 화면은 점수를 toFixed(1)로 적어 49.95~49.99가 "50.0"으로 보이면서 "잘 안 맞아요", 69.95~69.99가 "70.0"으로 보이면서 "무난해요"(B-5). NaN·null·undefined는 조용히 "잘 안 맞아요" | 예 — 경계 양쪽 값이 서로 다른 등급으로 나옴 | t-unit.mjs |
| W-FN-4 | 사람 확인 | 종목 성향 고르기(5번 호출 결과 중 선택): 동점, 일부 실패, 전부 실패 | BacktestPage.tsx:239-261 안의 이름 없는 코드라 따로 불러 볼 수 없어 가로챈 응답으로 확인. 동점(type 2·4 모두 70) → 먼저 오는 안정추구형이 "최고"이자 "가장 잘 맞는 종목"으로 표시(정렬이 안정적이라 번호 작은 쪽이 이김, 동점 표시 없음). 일부 실패(type 5) → 목록에서 조용히 빠짐. 전부 실패(5건 500) → "1년치 결과가 아직 계산되지 않아 비교할 수 없어요."(BacktestPage.tsx:815) — 서버 오류도 "아직 계산 안 됨"으로 말한다 | 해당 없음(정책 판단) | t-extra2.mjs |
| W-FN-5 | 실패 | 숫자·등락률·금액 포맷: 0, 음수, 매우 큰 수, null, undefined, 문자열 숫자 | null은 전부 "-"로 받는다. 그 밖: formatChangeRate(null)→"-" · formatChangeRate(undefined)→던짐 TypeError: Cannot read properties of undefined (reading 'toFi · formatChangeRate("123")→던짐 TypeError: rate.toFixed is not a function · formatChangeRate(NaN)→"NaN%" · formatChangeRate(Infinity)→"+Infinity%" · formatPrice(null)→"-" · formatPrice(undefined)→던짐 TypeError: Cannot read properties of undefined (reading 'toLo · formatPrice("123")→"123원" · formatPrice(NaN)→"NaN원" · formatPrice(Infinity)→"∞원" · formatVolume(null)→"-" · formatVolume(undefined)→"NaN만주" · formatVolume("123")→"123주" · formatVolume(NaN)→"NaN만주" · formatVolume(Infinity)→"∞만주" · formatTradingValue(null)→"-" · formatTradingValue(undefined)→"NaN만원" · formatTradingValue("123")→"0만원" · formatTradingValue(NaN)→"NaN만원" · formatTradingValue(Infinity)→"Infinity조" · toPercent(null)→"0.00%" · toPercent(undefined)→"NaN%" · toPercent("123")→"12300.00%" · toPercent(NaN)→"NaN%" · toPercent(Infinity)→"Infinity%" · formatBirth(undefined)→던짐 TypeError: Cannot read properties of undefined (reading 'spli · toKoreanDate("")→"년 0월 0일" · toKoreanDate(null)→던짐 TypeError: Cannot read properties of null (reading 'slice') · fromDashedYmd("")→"" · fromDashedYmd(null)→던짐 TypeError: Cannot read properties of null (reading 'replace'). src/utils/format.ts:12·17·27·38·50·68은 === null만 검사한다. undefined가 실제로 화면을 죽이는 것은 추가 발견 1에서 확인 | 해당 없음(실패) | t-unit.mjs · src/utils/format.ts, src/utils/date.ts:38-45 |
| W-FN-6 | 문서와 다름 | 장 시작 전 0 대체 규칙: 0인데 지난 값 있음 / 없음 / 진짜 0 | 대체 함수가 없다(홈이 DB 종가로 바뀌며 2026-09-18 제거, CLAUDE.md). 가장 가까운 보합 판정: 0 대체 함수 탐색→utils·api 어디에도 "지난 값으로 0을 대체"하는 함수가 없다(2026-09-18 제거, CLAUDE.md). 가장 가까운 것은 isFlatRate(보합 판정) · isFlatRate(0)→true · isFlatRate(0.004)→true · isFlatRate(-0.004)→true · isFlatRate(0.005)→false · isFlatRate(null)→false | 해당 없음 | t-unit.mjs |
| W-FN-7 | 실패 | 결과 설명 빈 문자열 대체: "", " ", null | submitTest(로그인 상태, 가로챈 POST): normal→{"type":"위험중립형","description":"서버 설명","imageUrl":"/s.png"} · empty→{"type":"위험중립형","description":"안정성과 수익성 사이에서 균형을 ","imageUrl":"/personality/neutral.svg"} · space→{"type":"위험중립형","description":"  ","imageUrl":" "} · null→{"type":"위험중립형","description":"안정성과 수익성 사이에서 균형을 ","imageUrl":"/personality/neutral.svg"} · unknown→{"type":"모름형","description":"서버 설명","imageUrl":"/s.png"}. ""·null은 프론트 문구·이미지로 대체, 공백만 있는 값은 그대로 통과(personality.ts:106-107, MypagePersonalityPage.tsx:82-83의 \|\| 연산). 서버가 모르는 성향 이름을 주면 대체표를 못 찾는다(PERSONALITY_INFO[이름]이 undefined) — 이번엔 설명이 있어 안 터졌지만 설명까지 비면 undefined.description을 읽는다(추측, 실행 안 함) | 예 — normal·empty·null 세 경우는 기대대로 갈림 | t-unit.mjs |
| W-R-1 | 통과 | 요청 중 다른 화면으로 옮겼을 때 끝난 요청이 새 화면을 덮는가 | 매개변수가 바뀌어도 같은 컴포넌트가 남는 곳 4곳은 모두 가드가 있다: StockChartPage(isStale), NewsList(isStale), BacktestPage(runSeqRef), AiPage(askSeqRef). 실측: 005930 응답을 2초 늦추고 000660으로 옮기면 끝까지 000660 제목·뉴스만 보인다. 가드가 없는 6곳(PersonalityTestPage, GuidePage, MypageProfilePage, HomePage, MypageLikesPage, MypagePersonalityPage)은 마운트 때 한 번만 부르는 effect라 새 화면을 덮을 경로가 없고, 언마운트 뒤 setState는 React 19에서 무시된다(C-2에서 경고 0건) | 예(약함) — 판정식에 옛 종목을 기대하면 거짓. 가드를 걷어낸 변이는 코드 수정 금지라 못 함 | t-race.mjs |
| W-R-2 | 실패 | ErrorBoundary 범위 트리 — 한 영역 실패가 화면 전체를 내리는 곳 | App.tsx:62 Header(경계 밖) / App.tsx:69 ErrorBoundary(resetKey=주소) > 74 Suspense > 75 Routes > 각 화면. 화면 안쪽 경계는 없다. 그래서 한 구역의 그리기 오류가 화면 전체를 오류 화면으로 바꾼다 — 실측 4곳: 백테스트 결과 구역(B-상태-null·지표null), 마이페이지 내 정보(M-상태-null), 홈 시세표 한 줄(추가 발견 1), 종목 상세 가격 줄(추가 발견 1) | 해당 없음(실패) | src/App.tsx:62-111 |
| W-R-3 | 통과 | useEffect 의존성 린트를 끈 곳 개수·위치·이유 | 1곳: BacktestPage.tsx:165. 이유 "investType은 일부러 뺀다. 사용자가 비운 뒤 다시 채워 넣으면 안 된다" — 의존성에 넣으면 사용자가 전략을 비울 때마다 추천 전략이 다시 채워지므로 뺀 이유가 코드와 맞다. @ts-ignore·@ts-expect-error 0곳 | 예 — grep이 1곳을 잡음 | grep |
| W-R-4 | 실패 | 데이터를 받는 컴포넌트마다 로딩/실패/빈 값 세 상태를 다루는지(O/X) | 아래 "W-R-4 표" 참조. X가 있는 곳: 홈 시세표 빈 값, 테마 카드 실패·빈 값(로딩 자리표시와 같음), 뉴스 더 보기 실패, 성향검사 보기 빈 값·저장 실패, 백테스트 결과 null, AI 답 빈 값, 내 정보 null, 투자유형 404, 관심종목 해제 실패 | 해당 없음(실패) | 블랙박스 결과 종합 |
| W-R-5 | 통과 | 목록에 인덱스를 key로 쓰는 곳 | 2곳. CardChart.tsx:230 거래량 막대(고정 길이, 순서 안 바뀜), NewsList.tsx:125 `${index}-${item.link}`(뒤에 붙이기만 하는 목록이고 같은 기사가 두 페이지에 걸쳐 올 수 있어 일부러 섞음 — 주석). 둘 다 순서가 바뀌거나 중간이 빠지는 목록이 아니다 | 예 — grep이 2곳을 잡음 | grep |
| W-D-1 | 통과 | mock·플래그 목록 — 켜진 것/꺼진 것 | API_DISABLED=false(client.ts:36) · USE_BACKEND_QUESTIONS=true(personality.ts:29, 실패 시 MOCK_QUESTIONS 7문항) · USE_BACKEND_GUIDE=false(guide.ts:13, MOCK_SECTIONS·MOCK_LATENCY 300ms 사용 중) · MOCK_SESSIONS·mockMessages(ai.ts:21,87, 대화 목록·내용은 항상 mock, 질문 ask만 실제) | 예 — grep이 각 선언을 잡음 | grep |
| W-D-2 | 통과 | 지어낸 값 생성기(랜덤·가짜 수익률) 흔적 | 0건. 검색식 backtestSample\|buildSamplePreset\|createPicker\|hashSeed\|pick(수,수). 이 브랜치에서는 ba536df가 아니라 dae25f3(2026-09-18)이 src/utils/backtestSample.ts를 지웠다(ba536df는 이 브랜치에 없음) | 예 — 같은 검색식이 ba536df 직전 트리에서는 18곳을 잡음(BacktestPage.tsx 3, backtestSample.ts 15) | git grep |
| W-D-3 | 통과 | 7문항 mock이 아직 참조되는지, 참조처 | personality.ts:59(USE_BACKEND_QUESTIONS가 false일 때 — 지금은 닿지 않음), personality.ts:68(서버 문항을 못 받았을 때 — 실제로 닿음, P-상태-500·끊김·null·빈배열에서 확인) | 예 — P-상태-500에서 1/7 화면이 뜸 | grep + 블랙박스 |
| W-D-4 | 통과 | 안 쓰는 export 목록(knip, package.json 수정 없이) | npx knip@5: 안 쓰는 export 3개 — src/api/stock.ts NEWS_PAGE_SIZE, src/utils/date.ts daysAgo, src/utils/personality.ts getScoreRange(파일 안에서만 씀) / 안 쓰는 export 타입 3개 — src/api/backtest.ts PresetOptions, src/api/stock.ts StockList, src/utils/backtest.ts Axis. 실행 뒤 git status 깨끗함 | 예 — 도구가 항목을 실제로 찾아냄 | knip |
| W-D-5 | 통과 | console.log 개수와 위치. 콘솔에만 찍고 화면에 안 알리는 곳 따로 표시 | console.log 0건. console.warn 22건, console.error 1건(ErrorBoundary). 콘솔에만 남고 화면엔 아무 표시가 없는 곳 7곳: NewsList.tsx:76(더 보기 실패), home.ts:66(카드 한 장 실패), personality.ts:67(예비 문항으로 전환), BacktestPage.tsx:139(종목 목록 → 예비 목록), BacktestPage.tsx:149(기간 옵션 → 프론트 표), HomePage.tsx:147(하트 되돌림), MypageLikesPage.tsx:68(해제 되돌림) | 예 — grep이 23곳을 셈 | grep + 블랙박스 |
| W-S-1 | 통과 | 없는 CSS Modules 클래스 참조 | "수정 작업 C 스크립트"가 이 저장소에 없어 같은 일을 하는 검사를 스크래치에 만들어 돌렸다(css-check.mjs). 없는 클래스 참조 0건. 정의만 되고 안 쓰는 클래스 3개: NewsList.module.css .notice, HomePage.module.css .note, MypageProfilePage.module.css .notice(BacktestPage는 styles[변수] 동적 참조라 제외) | 예 — 가짜 참조를 메모리에서만 섞자 27건을 잡음 | css-check.mjs |
| W-S-2 | 문서와 다름 | 커밋 e7baca9 이후에도 색·글자 크기를 직접 적은 곳 개수 | e7baca9는 이 브랜치에 없다(origin/feat/backtest-strategy-cards에만 있음). 그래서 커밋이 만들었다는 회색 단계·글자 크기 단계(fs-2xs~5xl)·굵기 단계가 이 브랜치 index.css에 없고, 색 토큰 8개(--color-*)만 있다(var(--color-*) 사용 300곳). 개수: index.css 밖 CSS에 색을 직접 적은 줄 62, font-size를 숫자로 적은 줄 194, TS·TSX 안 색 문자열 18(CandleChart.tsx 12, CardChart.tsx 6 — 차트 라이브러리·SVG에 넘기는 값) | 해당 없음 | css-check.mjs |
| W-B-1 | 통과 | npm run build 청크별 크기 | index 261.56KB(gzip 84.85) · StockChartPage 174.12KB(56.30) · BacktestPage 24.07KB(7.84) · AiPage 8.65KB · PersonalityTestPage 6.35KB · MypageProfilePage 5.45KB · 나머지 3KB 미만. CSS는 BacktestPage 12.18KB가 가장 큼. 화면별로 나뉘어(lazy) 첫 방문엔 index만 받는다 | 예 — 빌드 출력 그대로 | prep.log |
| W-B-2 | 문서와 다름 | 차트 라이브러리 두 벌(lightweight-charts·recharts)이 어느 청크에 얼마나 | recharts는 없다(2026-09-21 제거, 번들 검색 0건). lightweight-charts는 StockChartPage 청크에만 150.9KB(+ fancy-canvas 8.5KB), 지연 로드라 종목 상세를 열 때만 받는다. 소스맵 빌드를 스크래치 폴더로 따로 내 바이트를 나눴다(index 청크: react-dom 175.1KB, react-router 37.4KB, 앱 코드 27.7KB) | 해당 없음 | sm.mjs |
| W-B-3 | 문서와 다름 | personality.html과 본 앱이 공유 코드를 중복 번들하는지 | 진입점이 index.html 하나뿐이다(2026-09-21 personality.html 합침, CLAUDE.md). dist에 html 1개 | 해당 없음 | dist-sm |
| W-B-4 | 사람 확인 | npm audit 요약(high 이상) | high 1 · critical 0. nanoid 3.3.17(<3.3.18, GHSA-2v37-7h3g-55p8: size가 0인 사용자 생성기에서 무한 루프). 경로 vite → postcss → nanoid — 개발 도구 의존성이고 빌드 결과물에는 없다(dist 검색 0건). 직접 의존 아님, 고침 가능(fixAvailable) | 해당 없음 | npm audit |

## 추가 발견
지시서 항목 밖에서 나온 것. 판정표에 넣지 않았다. 심각한 순서.

| # | 내용 | 확인한 것 / 추측 | 근거 |
|---|---|---|---|
| 1 | 응답 필드 하나가 빠지면 화면 전체가 오류 화면 | 확인: 홈 목록 100종목 중 한 종목이라도 fluctuate나 closePrice 필드가 아예 없으면(값이 null이 아니라 키가 없음) 홈 전체가 "화면을 표시하지 못했어요"가 된다(TypeError: undefined.toFixed / undefined.toLocaleString). 종목 상세도 currentPrice·comparePrev가 빠지면 같다. 원인: API 계층이 응답을 그대로 넘기고(src/api/stock.ts:30-33, 69-75) 포맷 함수는 null만 거른다(src/utils/format.ts:12,17,27). 스웨거가 응답 필드를 필수로 적지 않아(W-API-6) 서버가 null 필드를 생략하도록 바뀌면 바로 닿는 경로다. 확인: 지금 백엔드(origin/dev)에는 null 필드 생략 설정(JsonInclude·NON_NULL·default-property-inclusion)이 없어 Jackson 기본값대로 null을 담아 보낸다 — 그래서 "키가 빠지는" 경우는 현재 서버에서 나올 가능성이 낮다. result 자체가 null인 경우(B-상태-null·M-상태-null)와는 별개다 | t-extra2.mjs |
| 2 | 기준일(baseDate) 하나가 null이면 시세표 전체가 실패 | 확인: 목록은 멀쩡한데 baseDate:null이면 "시세를 불러오지 못했습니다"로 100종목이 전부 사라진다. src/api/stock.ts:32의 fromDashedYmd(null)이 던진다 | t-extra2.mjs |
| 3 | 서버가 준 오류 문구를 그대로 화면에 띄움 | 확인: 백테스트는 모르는 코드의 실패면 서버 message를 그대로 보여준다(BacktestPage.tsx:85). 서버가 "java.lang.NullPointerException: Cannot invoke …"를 주면 그 글자가 사용자에게 보인다. 빈 message면 "요청 실패 (500)" | t-backtest-extra.mjs |
| 4 | 질문에서 종목명을 잘못 집음 | 확인: "LG에너지 솔루션"(띄어쓰기) → stockName "LG"(다른 종목)로 실린다. "sk하이닉스"(소문자) → 못 찾아 null. src/utils/stockName.ts:22-24는 원문 그대로 includes로 찾는다(검색창의 normalize와 달리 공백·대소문자를 다듬지 않음) | t-unit.mjs, A-1 |
| 5 | 생년월일 하한이 없다 | 확인: 0203-01-01, 1800-01-01, 오늘(2026-09-23)을 넣으면 그대로 PATCH가 나간다. 입력칸에 max만 있고 min이 없다 | dbg-m5.mjs |
| 6 | 형식이 틀린 종목코드도 요청을 보냄 | 확인: /stocks/abc를 열면 /api/stocks/abc/news 1건 + /api/stocks/abc 2건(5xx로 끝나 재시도)이 나간다. 추측: 서버가 이 경로에서 증권사 현재가 조회를 먼저 시도한다면 증권사 호출이 헛되게 늘 수 있다(서버 동작은 확인 안 함 — KIS 경로라 실제로 부르지 않았다) | S-1 |
| 7 | 백테스트 비교: 동점·전부 실패 문구 | 확인: 두 성향이 동점이면 번호 작은 쪽을 "가장 잘 맞는 종목"이라고 단정한다. 비교 5건이 모두 서버 오류여도 "1년치 결과가 아직 계산되지 않아"라고 말한다(BacktestPage.tsx:815). 일부 실패는 조용히 빠진다(W-FN-4) | t-extra2.mjs |
| 8 | 고른 전략 요청이 비교 요청과 겹쳐 실패 지점을 하나 더 만든다 | 확인: 기간이 1년이면 같은 요청(type 3·1년)이 두 번 나가고(B-3), 둘 중 두 번째만 실패해도 이미 받은 같은 데이터가 있는데 결과 전체가 실패 안내로 바뀐다(BacktestPage.tsx:239-253) | t-backtest-extra.mjs |
| 9 | 로딩 중과 값 없음이 같은 "-" | 확인: 종목 상세 가격 자리가 로딩 중에도 "-", 값이 null이어도 "-"다(S-2, S-6). 홈 카드는 로딩·실패·빈 값 셋이 "–"로 같다(H-3) | shot-S2-loading.png, shot-S6-dash.png |
| 10 | 조용한 대체 | 확인: 서버 문항을 못 받으면 예비 7문항으로(P-상태-500), 기간 옵션을 못 받으면 프론트 표로(B-상태-options500), 종목 목록을 못 받으면 예비 목록으로 바꾸면서 사용자에게 알리지 않는다. 결과 화면은 7문항 결과인지 10문항 결과인지 구분되지 않는다 | P-상태-*, B-상태-* |
| 11 | 문구와 실제가 다름 | 확인: 투자유형 빈 화면 "일곱 문항이면 끝나요"(MypagePersonalityPage.tsx:66) — 실제 문항 10개. personality.ts:14 주석 "확정된 문항은 7개"도 같다 | M-상태-성향null |
| 12 | AI 성향 조회 실패를 "성향 없음"과 같게 보임 | 확인: /ai에서 GET 성향이 500이어도 "투자성향을 정하면 나에게 맞춘 답을 받을 수 있어요. 투자성향 검사하기"가 뜬다. 성향이 있는 회원이 검사를 다시 하도록 유도될 수 있다 | A-상태-성향500 |
| 13 | 종목명이 빈 문자열이면 탭 제목이 "JUBY"만 | 확인: 화면 제목은 코드로 버티지만 탭은 "JUBY"(useDocumentTitle이 빈 이름이면 접미사만) | S-상태-종목명빈문자열 |
| 14 | 홈에 돌아올 때마다 카드 3건 재요청 | 확인: 홈→상세→홈을 오가면 매번 ONE_MONTH 카드 3건이 다시 나간다(S-8 순서 기록). CLAUDE.md의 "홈 카드 3회(증권사)"가 방문마다 반복된다 | S-8 |
| 15 | 안 쓰는 코드 | 확인: 안 쓰는 export 3·타입 3(W-D-4), 정의만 된 CSS 클래스 3(W-S-1) | knip, css-check.mjs |

## 부록: 준비 명령 출력
```
$ npm ci

added 34 packages, and audited 35 packages in 648ms

10 packages are looking for funding
  run `npm fund` for details

1 high severity vulnerability

To address all issues, run:
  npm audit fix

Run `npm audit` for details.
[exit 0]

$ npm run lint

> juby-fe@0.0.0 lint
> oxlint

[exit 0]

$ npx tsc -b
[exit 0]

$ npm run build

> juby-fe@0.0.0 build
> tsc -b && vite build

vite v8.2.0 building client environment for production...
[2Ktransforming...
A PostCSS plugin did not pass the `from` option to `postcss.parse`. This may cause imported assets to be incorrectly transformed. If you've recently added a PostCSS plugin that raised this warning, please contact the package author to fix the issue.
✓ 105 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                                   2.32 kB │ gzip:  1.19 kB
dist/assets/OAuthCallbackPage-DDNxrxbs.css        0.10 kB │ gzip:  0.11 kB
dist/assets/NotFoundPage-BlwfJjQ6.css             0.54 kB │ gzip:  0.33 kB
dist/assets/PersonalityCard-CsyVtSNo.css          0.63 kB │ gzip:  0.36 kB
dist/assets/GuidePage-BhJesMW0.css                1.13 kB │ gzip:  0.53 kB
dist/assets/MypagePersonalityPage-CaHFehxz.css    1.23 kB │ gzip:  0.54 kB
dist/assets/LoginPage-CpfeTSEl.css                1.29 kB │ gzip:  0.57 kB
dist/assets/MypageLayout-DCxE2hOF.css             1.43 kB │ gzip:  0.56 kB
dist/assets/PersonalityTestPage-DvguXded.css      1.87 kB │ gzip:  0.75 kB
dist/assets/MypageLikesPage-Bb8imkYs.css          2.05 kB │ gzip:  0.81 kB
dist/assets/PersonalityResultPage-BocktwT8.css    2.33 kB │ gzip:  0.86 kB
dist/assets/MypageProfilePage-BeYcW274.css        3.24 kB │ gzip:  1.04 kB
dist/assets/AiPage-Cq0fticI.css                   5.10 kB │ gzip:  1.59 kB
dist/assets/StockChartPage-BODuL5dD.css           5.77 kB │ gzip:  1.65 kB
dist/assets/index-BSnwGQm8.css                    9.37 kB │ gzip:  2.67 kB
dist/assets/BacktestPage-C0n-rzDQ.css            12.18 kB │ gzip:  2.69 kB
dist/assets/OAuthCallbackPage-Bk_eVezr.js         0.46 kB │ gzip:  0.35 kB
dist/assets/PersonalityCard-CeIddh9j.js           0.54 kB │ gzip:  0.32 kB
dist/assets/NotFoundPage-quGHkQjw.js              0.59 kB │ gzip:  0.42 kB
dist/assets/auth-DwXQqqk3.js                      0.65 kB │ gzip:  0.34 kB
dist/assets/GuidePage-YCfBoz6u.js                 1.10 kB │ gzip:  0.62 kB
dist/assets/PersonalityResultPage-vCDTsK-3.js     1.18 kB │ gzip:  0.64 kB
dist/assets/personality-HuO9JXY5.js               1.28 kB │ gzip:  0.67 kB
dist/assets/MypageLayout-DE0qaUrs.js              1.59 kB │ gzip:  0.86 kB
dist/assets/LoginPage-Uwp28-HD.js                 1.68 kB │ gzip:  0.86 kB
dist/assets/MypagePersonalityPage-Bad6KsX6.js     1.86 kB │ gzip:  0.96 kB
dist/assets/MypageLikesPage-BuAC3yfx.js           2.75 kB │ gzip:  1.25 kB
dist/assets/MypageProfilePage-D-wv78yn.js         5.45 kB │ gzip:  2.12 kB
dist/assets/PersonalityTestPage-B_nlZDC3.js       6.35 kB │ gzip:  2.53 kB
dist/assets/AiPage-6BQ2UMRm.js                    8.65 kB │ gzip:  3.47 kB
dist/assets/BacktestPage-CYMr59Ai.js             24.07 kB │ gzip:  7.84 kB
dist/assets/StockChartPage-I0jTpWdN.js          174.12 kB │ gzip: 56.30 kB
dist/assets/index-YcobNU2t.js                   261.56 kB │ gzip: 84.85 kB

✓ built in 377ms
[exit 0]
```
